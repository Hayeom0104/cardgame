# 테스트하기

압축을 푼 자리에서 그대로 돌아갑니다. 파이썬 3.11 이상이 필요합니다.

## 1. 설치

```bash
pip install -e ".[dev]"
```

## 2. 콘텐츠 만들기

```bash
python -m app.cli.bootstrap --db deckout.db      # 스키마 + 시드 + 발행
python -m app.cli.check_content --db deckout.db  # §10.5 검증
```

`deckout.db` 는 압축에 넣지 않았습니다. 위 명령이 새로 만듭니다.

## 3. 테스트

```bash
pytest -q            # 735개, 2분쯤 걸립니다
```

한 판을 처음부터 끝까지 눌러 보는 것만 돌리려면:

```bash
pytest tests/test_playthrough.py -q
```

이 파일은 `custom_id` 를 만들어 내지 않고 **화면이 돌려준 버튼만** 누릅니다.
중간에 누를 것이 없어지면 그 자리가 곧 구멍이라는 뜻입니다.

## 4. 서비스 띄우기

```bash
export DECKOUT_API_KEY=<중앙봇에서 발급받은 키>
export DECKOUT_CHANNEL_ID=<Deckout 채널 id>
export DECKOUT_CENTRAL_URL=http://<중앙봇 주소>
uvicorn app.api.server:app --port 8080
```

**두 환경 변수가 없으면 뜨지 않습니다.** 일부러 그렇게 했습니다 — 키가 없으면
계약을 모르는 채로 도는 것이고(§1.3.4), 채널이 없으면 런이 스레드를 못 열어
화면 없이 계정만 점유합니다(§1.3.5, §16.3). 둘 다 플레이어가 먼저 알게 되는
것보다 서비스가 안 뜨는 편이 낫습니다.

중앙봇 없이 코드만 보시려면 `pytest` 로 충분합니다. 위 테스트들이 중앙봇을
흉내 내는 스텁으로 전 구간을 지납니다.

## 5. 관리자 대시보드

```bash
export DECKOUT_ADMIN_PASSWORD=<긴 비밀번호>
export DECKOUT_ADMIN_SECRET=<세션 서명 키>   # 없으면 재시작 시 로그인 풀림
```

`http://localhost:8080/admin` — 비밀번호를 넣지 않으면 아예 켜지지 않습니다.

## 6. 수치 바꿔 보기

`config/*.toml` 열한 개가 게임의 모든 수치입니다. 설정마다 무엇을 바꾸는
값인지 바로 위에 한국어로 적혀 있고, 그 설명이 대시보드에도 그대로 나옵니다.

바꾼 뒤에는 콘텐츠를 다시 발행해야 반영됩니다:

```bash
python -m app.cli.bootstrap --db deckout.db
```

진행 중인 런은 시작할 때의 수치를 그대로 유지합니다 (§10.6).

## 7. 그림 넣기

`assets/<종류>/<id>.png` 로 넣으면 다음 화면부터 쓰입니다. 지금 들어 있는
것은 자리를 지키려고 만들어 둔 표식이라, 진짜 그림으로 덮으면 됩니다.

```bash
python -m app.cli.assets 없음     # 아직 없는 그림 목록
python -m app.cli.make_art        # 없는 것만 만들어 채우기
```

## 8. 코인 지급이 `operator_required` 로 갇혔을 때

중앙봇 응답 파싱이 틀려서(§17.3 규칙 8) 실제로는 지급이 끝났는데 로컬에서는
"적용액이 안 맞는다"고 판단해 갇힌 트랜잭션이 있을 수 있습니다. 기동 시
자동으로는 풀리지 않습니다 — 진짜 부분 지급과 구별할 방법이 코드에는 없어서,
사람이 확인하고 돌리는 도구로 남겨 뒀습니다.

```bash
python -m app.cli.reconcile_transactions --db deckout.db            # 전부
python -m app.cli.reconcile_transactions --db deckout.db --tx t_123 # 하나만
```

**새 금액을 청구하지 않습니다.** 같은 멱등성 키로 다시 물어봐서 중앙봇이
이미 저장해 둔 결과를 재생시킬 뿐입니다. 재조회해도 여전히 안 맞으면 진짜
이상 현상이라 그대로 `operator_required` 에 남습니다 — 그 경우는 중앙봇
쪽 기록을 직접 봐야 합니다.

## 알려진 것 하나

자동 플레이로 돌려 보면 **튜토리얼 보스를 52번째 시도에 처음 이깁니다.**
매 시도가 10~12라운드까지 가는 접전이라 승패가 사실상 운입니다. §3.4.1대로
튜토리얼 패배에는 비용이 없어 막히지는 않지만, 처음 하는 사람의 첫 한 시간
치고는 가혹할 수 있습니다.

§15 수치는 오너 결정 사항이라 건드리지 않았습니다. 조정하실 곳은
`config/01_전투.toml` 의 `enemy_bands` 와 `app/content/seed.py` 의 튜토리얼
보스 능력치입니다.
