# 변경 이력

이 파일은 여러 작업자와 AI가 같은 저장소를 사용할 때 변경 이유와 검증 상태를
공유하기 위한 기록입니다.

## 작성 규칙

- 모든 시간은 한국 표준시(KST, UTC+09:00)로 기록합니다.
- 코드, 설정, 테스트, 문서의 의미 있는 변경마다 날짜·시간, 요약, 검증 결과를
  새 항목으로 추가합니다.
- 커밋 또는 PR이 있으면 식별자를 함께 적습니다.
- 비밀값(API 키, 토큰, 비밀번호, 개인 정보)은 이 파일에 기록하지 않습니다.

## 2026-08-14

### 00:36 KST — 카드게임 서비스 포트 8087 설정

- 카드게임 FastAPI 서비스의 기본 수신 포트를 `8087`로 설정했습니다.
- `python -m cardgamebot.main` 실행 시 `.env`의 `CARDGAME_SERVICE_HOST`와 `CARDGAME_SERVICE_PORT`를 사용하도록 했습니다.
- 중앙봇 등록 주소는 `http://100.89.207.56:8087`으로 사용합니다.
- 중앙 Economy API 주소는 별도로 기존 `http://100.89.58.8:8080`을 유지합니다.
- 테스트는 실제 운영 API/채널 설정에 영향을 받지 않도록 중앙 API와 채널 제한을 격리했습니다.

검증: `pytest -q` 결과 67개 테스트 통과. 8090은 다른 프로그램이 사용 중이고, 8092는 Windows 예약 범위(8091–8190)라 8087로 변경했습니다.

실행 확인: CardGameBot이 `0.0.0.0:8087`에서 실행 중이며, `http://100.89.207.56:8087/healthz`가 `ok`를 반환했습니다.

### 00:26 KST — 중앙봇 테스트 환경 연결 확인

- 로컬 비공개 `.env`에 중앙봇 API 주소, 발급 API key, 테스트 채널 제한을 설정했습니다.
- 중앙봇의 `/healthz` 응답과 API key의 `read` 권한을 읽기 전용 요청으로 확인했습니다.
- 등록 채널 문맥의 `!덱아웃 정보` 이벤트가 중앙 사용자 조회를 거쳐 `reply` action을 반환하는 것을 확인했습니다.
- `.env`는 `.gitignore`로 제외되어 있어 비밀값은 GitHub에 커밋되지 않습니다.

### 00:09 KST — 변경 이력 도입

- 여러 작업자가 변경 내용을 추적할 수 있도록 `CHANGELOG.md`를 추가했습니다.
- 이후 Codex가 수행하는 의미 있는 변경은 이 파일에 먼저/함께 기록합니다.

### 00:05 KST — 중앙봇 HTTP 연동 계약 반영

- 중앙봇의 `POST /event` 메시지 형식(`raw_content`, `command`, `args`)을 처리하도록 변경했습니다.
- 응답을 중앙봇 action 형식(`reply`, `ignore`, `reply_ephemeral`)과 PNG `attachments[].data_b64` 형식으로 전환했습니다.
- `GET /healthz`, `POST /shutdown`을 추가했습니다.
- Economy API 호출을 `X-API-Key`, `idempotency_key`, 실제 적용 금액 검증 방식으로 변경했습니다.
- 명령 루트를 `!덱아웃`으로 전환하고, 기존 `!카드`는 호환용 별칭으로 유지했습니다.
- 중앙봇 계약 및 Economy 호출 테스트를 추가했습니다.

검증: `pytest -q` 결과 67개 테스트 통과.

커밋: `83a4e75 Integrate Central Bot HTTP contract`
