# 변경 이력

이 파일은 여러 작업자와 AI가 같은 저장소를 사용할 때 변경 이유와 검증 상태를
공유하기 위한 기록입니다.

## 작성 규칙

- 모든 시간은 한국 표준시(KST, UTC+09:00)로 기록합니다.
- 코드, 설정, 테스트, 문서의 의미 있는 변경마다 날짜·시간, 요약, 검증 결과를
  새 항목으로 추가합니다.
- 커밋 또는 PR이 있으면 식별자를 함께 적습니다.
- 비밀값(API 키, 토큰, 비밀번호, 개인 정보)은 이 파일에 기록하지 않습니다.

## 2026-09-09

### 22:30 KST — 모바일 런 화면·무료 컬렉션 UI·튜토리얼 온보딩 개편

- 런 시작 시 비공개 스레드 첫 메시지에서 플레이어를 멘션하도록 했습니다.
- 전투는 조작용 `내 턴` 이미지와 `전황·전투 기록` 이미지를 별도 메시지로
  보내도록 바꿨습니다.
- 지도는 모바일 세로형 경로, 보석형 노드, 명확한 진행 상태색으로 개편했습니다.
- 원화 없이도 신규 배너가 비어 보이지 않게 무료 카르타·속성 크레스트·픽업
  타이포그래피를 넣었습니다. 메인 허브는 재화·진행도·출석을 한 화면에 간단히
  요약합니다.
- 수 속성 딜서포트형 2★ `리라`와 전용 수 카드 3장을 추가했습니다. 캐릭터
  일러스트는 의도적으로 넣지 않아 이후 원화를 그대로 연결할 수 있습니다.
- 튜토리얼은 적 1마리 고정, 일반 적 22–30 HP/6–8 공격, 보스 72 HP/7–9 공격으로
  낮춰 첫 플레이에서 규칙을 배우고 통과하도록 조정했습니다.

검증: 콘텐츠·보스 페이즈·전투 렌더/API 관련 테스트 84개 통과, 렌더/API·콘텐츠·보스
페이즈 재검증 44개 통과.

### 17:43 KST — 월드 1 고블린 원화 6종 교체 및 프롬프트 문서 분리

- 고블린·방패병·주술사·늑대조련사·돌격병·대족장 이미지를 실제 몬스터
  원화로 교체했습니다. 역할별 실루엣은 유지하면서 무기 궤적, 속성광,
  전장 배경을 더해 기존 카드 아이콘보다 화려한 연출로 맞췄습니다.
- 신규 캐릭터 NovelAI 프롬프트는 오너에게 별도 파일로 전달하고 프로젝트
  저장소에서는 삭제했습니다.

검증: 6개 PNG가 모두 140×140 규격이며 합본 미리보기로 역할 구분과
크롭 상태를 확인했습니다. 테스트 러너는 현재 셸의 Python 환경에서
`pytest` 모듈을 찾지 못해 재실행하지 못했습니다(직전 코드 커밋 848개 통과).

### 10:00 KST — 1차 카드 배치 48장과 신규 캐릭터 5명 추가

- Design Addendum A-2의 전제에 맞춰 가챠 캐릭터를 7명에서 12명으로 확장했습니다.
- 화·수·풍·지·광·암 카드 6장씩과 무속성 카드 12장, 총 48장을 추가했습니다.
- 카드 배치 검사를 기본 활성화하고 실제 시드 콘텐츠가 검사를 통과하도록 했습니다.
- 모든 카드에 과도한 장식을 줄인 2D 스킬 일러스트를 제작해 연결했습니다.
- v7 문서와 달랐던 기존 시드 캐릭터의 역할·성급·6성 가능 표시를 수정했습니다.
- 신규 캐릭터 5명의 NovelAI 태그식 제작 프롬프트를 문서화했습니다.

검증: 카드 배치·콘텐츠·가챠 관련 테스트 161개 통과, 전체 추적 테스트
848개 통과, bootstrap 및 §10.5 콘텐츠 검사 통과.

## 2026-09-01

### 13:59 KST — UI 전면 개편 4탄: 전투 화면 첨부 두 장을 다시 나눔

오너 지시로 전투 화면의 PNG 두 장(§1.3.7 한도) 구성을 바꿨다.

- **고치기 전**: [적+아군+전투기록을 세로로 합친 1장(`deckout_combatants.png`)] +
  [손패 1장(`deckout_hand.png`)].
- **고친 후**: [적 상태 + 전투 기록(`deckout_situation.png`, "누가 싸우는가·
  무슨 일이 있었는가")] + [아군 상태 + 손패·장착 패시브·자원
  (`deckout_turn.png`, "내가 뭘 낼 수 있는가")].

아군 상태와 손패를 같은 장에 묶은 이유: 카드를 낼지 말지 판단하려면 내
카드뿐 아니라 지금 내 체력·방어막·상태이상도 같이 봐야 한다 — 둘이
다른 장에 떨어져 있으면 매번 오가며 봐야 했다.

검증: 하드코딩된 옛 파일명(`deckout_combatants.png`/`deckout_hand.png`)을
쓰던 테스트 세 개(`tests/test_render_and_api.py`,
`tests/test_assets_and_screens.py`, `tests/test_battle_render_playthrough.py`)를
새 이름에 맞춰 고침. 렌더한 PNG 두 장을 오너에게 전달해 확인. 전체
pytest 848 passed. bootstrap/check_content 통과.

## 2026-08-27

### 15:56 KST — UI 전면 개편 3탄: 허브를 오너 참고 이미지대로 재설계 (프로필 사진 + 아이콘)

오너가 참고 이미지를 주고 그대로 짜 달라고 지시했다. 세 가지를 새로 했다.

**① 실제 디스코드 프로필 사진을 초상으로.** 대표 캐릭터 그림 대신
플레이어 본인의 프로필 사진을 쓴다(오너가 "디스코드 프로필사진"이라고
명시). `app/central/avatar.py` 신규 — Central의 `GET /v1/users/{id}`
응답에서 흔한 후보 필드 이름(`avatar_url`, `avatar`, `icon_url` 등)을
순서대로 찾는다. 이 저장소엔 연동 가이드 원문이 없어 정확한 필드명을
모른다 — 기존 `COIN_BALANCE_KEYS`/`DISPLAY_NAME_KEYS`와 같은 방식이다.
**필드명은 추측이니 실제 가이드로 확인이 필요하다고 오너에게 안내함.**
그림을 받는 것은 렌더 경로에 새로 들어온 네트워크 I/O라 실패를 전부
삼킨다 — 타임아웃 0.8초, 2MB 초과·그림이 아닌 응답·네트워크 오류 전부
조용히 기존 캐릭터 그림으로 물러난다(§11, "그림이 없어도 화면은
나가야 한다"). `handlers.hub_screen`에서 `try/except Exception`으로
한 번 더 감쌌다 — 무엇이 터져도 화면 자체는 반드시 나가야 해서다.

**② 플레이어 ID는 넣지 않았다.** 오너가 참고 이미지에는 있지만 빼
달라고 명시했다.

**③ 화면을 참고 이미지 짜임대로.** 각진(둥근 모서리) 초상, 카르타·
와일드카드에 아이콘 배지, 캐릭터·파티 슬롯·패시브 슬롯·업적을 게이지
타일 대신 아이콘+구분선 한 줄로, 출석 배너에 원형 아이콘 배지와
화살표. 번들 글꼴이 유니코드 심볼 글리프(⚑◆✓ 등)를 못 그리고
`kit.sanitize()`도 그 구간을 걸러 내므로, 아이콘은 전부 선·원·다각형만
쓰는 벡터로 새로 그렸다 — `app/render/kit.py`에 `icon_person`,
`icon_group`, `icon_gem`, `icon_flag`, `icon_shield`, `icon_check_badge`,
`icon_calendar` 추가.

검증: `tests/test_avatar.py` 신규(후보 키 탐색, HTTP 오류·오버사이즈·
비이미지 응답·빈 응답 전부 None으로 물러나는지). `tests/test_onboarding_hub.py`에
프로필 사진 연결·미연결·연결 도중 예외 발생 세 경로 추가. 전체 pytest
848 passed(무관한 파일 `test_run_loop.py`의 확률적 테스트가 한 번
우연히 걸렸던 것은 재실행 5회 전부 통과로 이번 변경과 무관함을 확인 —
서버 난수 지도 시드로 6개 칸 종류가 12번의 무작위 표본에 전부 나오길
기대하는 원래도 드물게 흔들릴 수 있는 테스트, 이번 세션에서 고치지
않음). bootstrap/check_content 통과. 렌더한 PNG(사진 없음/있음 둘 다)를
오너에게 전달해 확인.

## 2026-08-26

### 15:12 KST — UI 전면 개편 2탄: 허브 버튼을 트리 구조로 재편

오너 지시로 허브 버튼을 다음 트리로 다시 짰다:

```
시작
관리     → 덱 관리 · 캐릭터 · 연구 · 장비 · 업적
상점     → 상점 · 뽑기
```

[시작]만 최상위에 바로 서고, 나머지는 [관리]/[상점] 두 카테고리 버튼을
눌러야 하위 목록이 펼쳐진다(+ 각 하위 목록엔 "◀ 뒤로"). 예전엔 7개
버튼이 한 화면에 쭉 늘어서 있었다 — `app/api/handlers.py`의 `_HUB_TREE`
(이전 `_QUICK_ACTION_BUTTONS`)로 구조를 옮기고, `hub.py`의
`handle_hub`에 카테고리 펼치기/뒤로 가기 분기를 추가했다. 카테고리
버튼을 눌러도 화면 그림은 다시 그리지 않고 버튼 줄만 바꿔 끼운다(중앙봇
왕복 없이 즉시 반응) — `equip_prompt`가 이미 쓰던 것과 같은 패턴.

**같이 잡힌 실제 버그 두 개** (이번 개편으로 발견):

1. **장비 버튼이 아예 없었다.** `!덱아웃 장비` 텍스트 명령으로만
   갈 수 있었고 허브 버튼 목록엔 빠져 있었다. [관리] 밑에 넣으면서
   `hub._QUICK_ACTIONS`에 `"eq": "equipment_screen"`을 추가해 고쳤다.
2. **강제 튜토리얼 게이트가 버튼에는 안 걸렸다.** `handle_message`의
   `_ALLOWED_BEFORE_TUTORIAL`은 텍스트 명령만 막고, 허브 버튼은
   `hub.handle_hub`가 화면 함수를 직접 불러 이 게이트를 건너뛴다 —
   즉 튜토리얼을 안 깬 계정도 허브 버튼만 누르면 뽑기·상점 등에
   들어갈 수 있었다(신고된 적 없는 구멍, 이번에 트리를 짜다가 발견).
   `_quick_action_components`가 튜토리얼 미완료 계정에는 [시작]만
   보여주도록 하고, `hub.py`에도 같은 게이트를 다시 검사하는 코드를
   추가했다 — 화면이 안 보여줘도 custom_id는 위조할 수 있어서다.

검증: `tests/test_onboarding_hub.py`의 관련 테스트를 트리 구조에 맞춰
다시 쓰고(카테고리 펼치기·뒤로 가기·소유권 대조·튜토리얼 게이트 위조
시도), 전체 pytest 스위트 835 passed. bootstrap/check_content 통과.
실제 클릭 경로(관리→장비→뒤로)를 직접 실행해 확인.

### 14:38 KST — UI 전면 개편 착수 1탄: 색 버그 2개 + 허브 화면 재설계

오너가 "UI를 전체적으로 갈아엎겠다"고 지시해 화면들을 실제로 렌더링해서
눈으로 확인하며 시작했다. 이번 세션에서 끝난 것과, 계속 이어갈 것을
구분해 둔다 (Task #33, 진행 중).

**전역 버그 두 개** (모든 화면에 영향):

1. `config/10_화면.toml`의 `color_by_element`가 실제 원소 순환(§2.10:
   화·수·풍·지·광·암·무속성)과 안 맞았다 — 풍·지가 아예 없고, 대신 쓰지도
   않는 오행(목·금·토)이 들어 있었다. 풍·지 속성 카드는 그동안 전부
   color_muted(회색)로 뭉개져 그려지고 있었다 — 손패 스크린샷으로 확인
   (`화염 강타`는 빨강, `대지 가르기`는 회색). 풍·지에 색을 채워 넣어 고쳤다.
2. 지도 화면에서 보스 칸만 유일하게 한자 "王"을 썼다("보상"/"보스"가
   첫 글자 "보"로 겹쳐서 넣은 예외 처리였다). 마지막 글자("스")로 바꿔
   한글만으로 구분하도록 고쳤다 — `app/render/panels.py`.

**허브 화면 재설계** — 이번 개편의 첫 화면. 예전 판은 초상 옆에 굵기·크기
구분 없는 텍스트 6줄을 쌓아 놓은 모양이라, 무엇이 중요한 정보인지 한눈에
안 들어왔다(오너가 지적한 바로 그 문제). 세 층으로 다시 짰다:

- ① 프로필 패널 — 원형 초상 + 이름(큰 글자) + 재화 네 개(코인·실버·카르타·
  와일드카드)를 알약 배지로 한 줄에.
- ② 진행도 — 캐릭터·파티 슬롯·패시브 슬롯·업적을 같은 모양 타일 네 개로,
  각각 "3/8" 숫자와 함께 실제 게이지 바를 그려 비율이 눈에 보이게.
- ③ 출석 배너 — 받을 게 있으면 강조 테두리로 눈에 띄게, 이미 받았으면
  눌러 앉힌 무채색으로.

허브만 쓰던 `panel_size`(720×360, 원래 전투 패널용) 대신 `hub_size`
(820×400)를 새로 정의했다 — 세 층을 담기엔 비좁았다.

검증: `tests/test_render_and_api.py`·`test_onboarding_hub.py`·
`test_playthrough.py` 재실행, 전체 pytest 스위트 831 passed.
bootstrap/check_content 통과. 렌더한 PNG를 직접 오너에게 보내 확인.

지도 화면은 색만 고쳤고 레이아웃(칸이 세로 가운데에 몰려 위아래가 크게
빈 문제)은 아직 손대지 않았다 — 다음에 어느 화면부터 이어갈지 오너
확인 대기 중.

### 13:50 KST — 설계 문서 부록 A-2: 카드 배치 1 형태를 §10.5 검사로 추가

오너가 업로드한 `Deckout_Design_Addendum_A2_Card_Batch1_Scope.md`는 카드
배치 1의 목표 형태(캐릭터 전용 36장 = 12명×3장, 무속성 12장)만 정하고
실제 카드 이름·효과·그림은 염염 님이 대시보드에서 따로 저작한다고
명시한다. 실제 로스터를 대조해 보니 가챠풀 캐릭터가 12명이 아니라
7명(스타터 제외 — R3의 "Characters: 8"과 일치)이라는 전제 불일치를
발견해 오너에게 확인했고, "§10.5에 배치1 형태 검증 규칙 추가"로
답을 받았다.

**한 것**: `config/12_카드_배치1.toml`(신규)에 배치 1의 비용·등급
분포 목표를 §15 상수로 두고, `app/content/validation.py`에
`_validate_card_batch1_shape`를 추가해 §10.5 발행 검사에 연결했다.
`batch1_shape_enforced`는 **기본값 false** — 지금 켜면 로스터도 12명이
아니고 카드도 저작 전이라 기존 플레이스홀더 콘텐츠부터 막힌다. 로스터가
채워지고 배치 1이 실제로 저작되면 오너/콘텐츠 담당이 이 값을 true로
바꿔서 켠다.

캐릭터 단위가 아니라 **전체 합**으로 검사한다 — `cards` 테이블에 카드를
특정 캐릭터에 묶는 자리가 없고(원소만 있음), 여러 캐릭터가 같은 원소를
공유하기도 해서다(예: 테라돈·실바 둘 다 지). A-2.2 자체가 "이번 배치는
캐릭터별 편차 없음"이라고 명시하므로 인구 수준 합계 검사가 실제로
확인하려는 것과 맞다. §5.6 등급 구간(기본1-3/중간4-5/최고6)은 이미
`config/04_뽑기.toml`의 `gacha_band_rarity_tiers`에 있어 새로 만들지
않고 그대로 읽었다. 최고등급(6티어)은 A-2.4대로 검사 대상에서 뺐다.

검증: `tests/test_card_batch1_shape.py` 10개(정상 형태 통과, 각 축의
위반 각각 거부, 검사 꺼짐 상태에서 기존 콘텐츠 안 깨짐, 최고등급 제외
확인). 작성 중 함수를 `_validate_constants` 중간에 잘못 끼워 넣어 그
뒷부분이 고아가 되는 버그를 만들었다가 직접 재현해 잡았다 — 최종본은
`_validate_constants` 뒤, `_effects` 앞에 올바르게 있다. 전체 pytest
스위트 재실행, bootstrap/check_content 통과.

## 2026-08-25

### 15:32 KST — R3 검증 보고서: 두 BLOCKER(B-01/B-02)와 세 MAJOR(M-01/M-02/M-03) 조치

오너가 업로드한 `Deckout_Existing_Implementation_Gameplay_Verification_R3.md`가
지적한 항목을 실제 코드에 대조해 확인하고 고쳤다. G-01(한글 글꼴)은
어제 이미 고쳤고(commit `89233e2`), G-02(최종 에셋)는 콘텐츠 제작
작업이라 코드로 손댈 것이 없다 — 이 항목은 손대지 않았다.

**B-01 — 강제 튜토리얼이 사실상 클리어 불가능** (BLOCKER)

보고서는 "가장 강한 카드만 골라도 1,500회 조작·140연패 동안
클리어 없음"을 재현했다. §15.9의 완결된 튠tempo 계산(스킬
`floor(10×1.8−4)=14`, 평타 6, 보스 피해 `11.5−6=5.5`/턴 — **어디에도
속성 상성 배율이 없다**)과 실제 시드를 대조한 결과, `enemy_tut_박쥐`와
`enemy_tut_boss`가 '암' 속성으로 시드돼 있었다. 이 값은 §15.9 표
어디에도 없다 — 튜토리얼 적 속성은 문서가 정하지 않은 값이었는데,
누군가 '암'을 골라 넣은 것이다. 문제는 스타터 루야가 §4.6.1에 **명시된**
'화' 속성이라는 점: 화→암은 상성상 최악의 조합이라(스킬 -25%, 받는
피해 +50%) 문서의 계산이 완전히 무너졌다. 게다가 튜토리얼 보스에만
문서에 없는 2페이즈 전용 기절기(`act_boss_기절`, 14dmg+기절)가 붙어
있었는데, §15.9의 계산은 그 전 구간 내내 평타만 가정한다 — 보스가
가장 위험해지는 시점(50% 미만)에 정확히 플레이어의 턴을 통째로
날리는 발명된 메커닉이 얹혀 있던 셈이다.

**고침**: `enemy_tut_박쥐`/`enemy_tut_boss` 속성을 '무속성'으로
되돌리고(§15.9가 가정한 상성 없음과 정확히 일치), 튜토리얼 보스의
`act_boss_기절` 페이즈2 규칙을 제거했다(§15.9가 아예 존재를 언급하지
않는 능력). 새로 만든 값을 발명하지 않고, 문서가 이미 계산해 둔
숫자가 성립하도록 **문서에 없던 값만** 제거한 것이다.

몬테카를로로 실측: 렌더를 건너뛰고 실제 화면 클릭 경로(`Session`)로
30판을 돌린 결과 — 고치기 전 20/20 전부 400회 예산 소진(`run_abandoned`,
0% 클리어), 고친 뒤 27/30 클리어(90%, 평균 84회 조작). `tests/test_boss_phases.py`의
"phase-gated action" 테스트는 튜토리얼 보스 대신 본편 보스
(`enemy_w1_boss`)로 옮겨 `min_phase` 엔진 동작 자체는 계속 검증한다.

`tests/test_playthrough.py`에 R3 §9 필수 인수 테스트 A를 새로 추가했다
(`test_a_fresh_player_can_actually_clear_the_tutorial_and_reach_the_main_campaign`)
— 보스 HP를 직접 0으로 만들거나 포기를 정상 종료로 받아들이지 않고,
화면이 준 컨트롤만으로 실제로 이겨서 `tutorial_completed_at`·본편
해금·첫 뽑기 보장(10연)·본편 준비 화면까지 끝까지 확인한다. 재시도
상한 5회(실측 90% 단판 클리어율 기준 — "수십 번의 통계적 재시도"가
아니라 이 상한 자체가 R3이 요구한 "승인된 재시도 상한"이다).
기존 `test_a_tutorial_run_never_gets_stuck`는 포기를 정상 종료로 받는
이유(화면이 막히는가만 본다)를 docstring에 명시하고 균형 검증은 새
테스트로 분리했다.

**B-02 — 응답 유실 뒤 화면 복구 불가** (BLOCKER)

`POST /event`는 핸들러를 커밋한 뒤 `processed_events`에 `event_id`만
기록하고(`run_id`는 항상 `None`) 응답을 돌려줬다. 응답이 유실되면
재전송은 그냥 `{"action": "ignore", "duplicate": true}`였고, 오래된
버튼은 리비전 불일치로 거절되고, `!덱아웃`은 스레드로의 redirect뿐
새 화면을 밀어주지 않았다 — 플레이어는 서비스 재시작 없이는 빠져나갈
길이 없었다.

**고침**:
- `record_event`에 실제 `run_id`를 기록한다(`lifecycle.most_relevant_run_id`
  — 활성 런이 있으면 그것, 방금 끝난 런이면 그 유저의 가장 최근 런).
- 중복 `event_id` 수신 시 `lifecycle.run_for_event`로 그 런을 찾아
  `handlers.current_screen()`이 **이미 커밋된 상태만으로** 지금 화면을
  다시 만들어 돌려준다(지도/전투/보스전/상점/보상/이벤트 분기 지원).
  재구성할 수 없는 상태(중첩 정지 화면)만 기존 `ignore`로 물러난다.
  로직을 재실행하지 않는다 — `surfaces._current_screen_payload`(§16.8
  기동 복구)와 같은 안전 규칙.
- 같은 `current_screen()`을 스테일 컴포넌트 거절에도 재사용했다
  (`gates.StaleComponentError`/`lifecycle.StaleRevisionError`) — 리비전이
  밀린 버튼이라도 그 버튼이 붙은 메시지는 여전히 유효하므로, 텍스트뿐인
  "다시 시도해 주세요" 대신 지금 화면을 그대로 돌려준다. `presentation_revision`을
  건드리지 않으므로 §16.6 단일 기록자 규칙과 충돌하지 않는다.
- `!덱아웃`의 "이미 진행 중" redirect는 **손대지 않았다** — `push_frame`은
  "끝난 런에만 쓴다"고 스스로 문서화돼 있고(§16.6), 진행 중인 런에 억지로
  쓰면 인터랙션 응답과 경합하는 두 번째 기록자가 생긴다. 발명하는 대신
  남겨 둔다.

검증: `tests/test_render_and_api.py`에 실제 FastAPI `TestClient`로
R3 §9 필수 인수 테스트 B를 그대로 재현하는 테스트를 추가했다 — 노드
클릭 커밋 → 같은 `event_id` 재전송 → 살아 있는 전투 컨트롤을 받아
실제로 눌러 진행까지 확인. `tests/test_playthrough.py`에 스테일
컴포넌트 클릭 테스트도 추가.

**M-01 — Central 호출이 이벤트 예산을 넘길 수 있음** (MAJOR)

허브가 `_coin_line()`과 A-1.2 대시보드에서 각자 `_central_user()`를
불러 `GET /v1/users/{id}`가 한 응답에 두 번 나갔다(지난 세션에 반쪽만
고쳐졌던 자리). `profile`을 함수 맨 앞에서 한 번만 읽어 두 곳 모두에
넘기도록 고쳤다. `CentralClient`의 기본 타임아웃도 5.0초 → 1.5초로
낮췄다(§1.2 인터랙션 예산 2.0초보다 짧게, `app/config.py`의
`CENTRAL_CLIENT_TIMEOUT_SECONDS`로 뺐다). 검증: `test_the_hub_fetches_the_central_profile_only_once`
추가.

**M-02 — 인박티비티 만료가 스레드 안 버튼 조작에는 적용 안 됨** (MAJOR)

`check_gates()`가 `is_expired()`를 아예 확인하지 않아, 30분 방치된
스레드의 예전 버튼을 누르면 `claim_mutation()`이 `last_activity_at`을
갱신해 방치된 런을 되살렸다. `check_gates()`에 만료 확인을 추가하고
(`gates.RunExpiredError`), 걸리면 그 자리에서 §16.3 정산을 실행한 뒤
스레드 화면도 정리된 상태로 고쳐 쓴다. 검증:
`test_a_stale_in_thread_component_click_settles_the_run_as_expired`
추가(R3 §9 필수 인수 테스트 D의 앞부분).

**M-03 — 종료된 런의 개인 스레드 정리가 구현되지 않음** (MAJOR)

`terminal_thread_retention_hours`는 설정돼 있었지만 아무도 읽지
않았고 `CentralClient.delete_thread()`는 어디서도 불리지 않았다.
`lifecycle.threads_due_for_cleanup`/`cleanup_terminal_threads`를 새로
만들고, 운영자가 직접 돌리는 `python -m app.cli.cleanup_terminal_threads`를
추가했다(기동 시 자동 실행은 하지 않는다 — Discord 스레드 삭제는
외부에 보이는 동작이라 `reconcile_transactions.py`와 같은 이유로
운영자 판단에 맡긴다). `runs.thread_deleted_at` 컬럼을 추가해
이미 정리한 것을 다시 시도하지 않는다(스키마 v8, `MIGRATIONS[8]`).

`delete_thread()` 자체의 요청 필드는 **바꾸지 않았다** — R3는 가이드
원문에 `delivery_request_id`/`service_id`가 있다고 적었지만, 이
저장소가 가진 v6.4/v7 설계 문서(§1.3.5, "Retain the thread for 24
hours, then DELETE... reason, expected_thread_id, expected_surface_generation
supplied")는 지금 코드와 정확히 일치하는 필드만 보여준다. 두 소스가
서로 다른 내용을 말하고 있어 어느 쪽이 맞는지 이 세션은 확인할
방법이 없다 — 실제 가이드 원문(`중앙봇_API_연동_가이드_업데이트`) 없이
필드를 지어내지 않았다. 오너 확인이 필요하다.

검증: `tests/test_terminal_thread_cleanup.py` 신규(정상 삭제·
already_missing·stale_generation·재시도 안 함·부분 실패 격리).
전체 스위트 재실행, 초록. `python -m app.cli.bootstrap` (schema v8),
`python -m app.cli.check_content` 통과.

### 09:40 KST — 한글이 전부 네모로 깨지는 버그 수정: 글꼴 파일을 저장소에 번들

오너가 실제 화면 스크린샷을 보내 줬는데, 카드 이름을 포함한 모든 한글이
□로 깨져 있었다(숫자·영문은 정상).

`app/render/theme.py`의 `_load_font()`는 이 상황을 이미 예상하고
있었다 — `font_candidates`(§10_화면.toml) 순서대로 찾되 **실제로 한글이
그려지는지**(`_supports_hangul`, 네모와 실제 글리프의 렌더 결과를
비교)까지 검사하고, 하나도 없으면 경고 로그를 남기고 Pillow 기본 글꼴로
물러난다. 그런데 목록의 첫 자리 `assets/fonts/main.ttf`가 `.gitkeep`만
있는 빈 폴더였고, 나머지 후보는 전부 **배포 서버에 설치돼 있어야 하는
시스템 글꼴 경로**(NanumGothic, NotoSansCJK, wqy-zenhei, 일본어
고딕)였다. 이 세션의 샌드박스에는 우연히 `wqy-zenhei.ttc`가 깔려 있어
지금까지 렌더 확인이 전부 정상으로 보였지만, 오너의 실제 배포 환경에는
그 후보 전부가 없어서 진짜 마지막 대비책(Pillow 기본 글꼴, 한글
글리프 없음)까지 떨어진 것이었다.

**고침**: 시스템 글꼴에 기대지 않도록 `assets/fonts/main.ttf`에 실제
한글 지원 글꼴을 저장소에 직접 넣었다 — WenQuanYi Zen Hei
(Debian `fonts-wqy-zenhei`, GPL-2 + font embedding exception이라
자유롭게 임베드 가능, 출처는 `assets/fonts/NOTICE.md`). 이제 배포
환경이 무엇이든 첫 번째 후보에서 항상 성공한다.

검증: 허브 대시보드를 실제로 렌더해 한글이 정상적으로 나오는지 눈으로
확인. `tests/test_assets_and_screens.py`의 글꼴 관련 테스트(폴백,
한글 지원 검사) 포함해 재실행, 전체 스위트 통과.

### 06:40 KST — 설계 문서 부록 A-1 구현: 가입 화면, 허브 대시보드, 도움말

오너가 업로드한 `Deckout_Design_Addendum_A1_Onboarding_Hub.md`를 v7의
§4.6.5/§19.1/§19.3을 확장하는 문서로 반영했다. v7을 다시 열지 않고
그 위에 얹는 구조라, 기존 §4.6.5 코드와 이번 addendum이 서로 다른 걸
요구하는 부분(가입 뒤 곧장 튜토리얼로 들어가는가)은 addendum 쪽을
따랐다 — "새 동작"으로 명시돼 있고, 지난번 §4.6.5 이탈(명시적 가입
버튼)과도 방향이 같다(강제 튜토리얼).

**A-1.1 가입 흐름**

- `dko:reg:go` → `dko:hub:join:<user_id36>`로 옮겼다. 가입 화면이
  Deckout 채널의 **공개** 메시지라 다른 사람도 버튼을 볼 수 있어서다 —
  custom_id에 대상 user_id36을 담고, `hub.handle_hub`가 누른 사람과
  대조해 다르면 `본인의 게임이 아닙니다.`로 거절한다(§19.2 다섯 게이트
  중 소유권 게이트에 해당).
- 가입을 누르면 계정을 만들고 **곧장 튜토리얼 런으로 들어간다**
  (`handlers.start_run`을 그대로 재사용 — 새 계정은 항상 튜토리얼
  분기를 타므로 별도 코드가 필요 없었다). 중복 클릭 경합으로 계정이
  이미 있었다면 새로 만들지도, 다시 안내하지도 않고 조용히 허브만
  보여준다.
- **강제 튜토리얼 게이트를 다시 손봤다**: 가입이 런을 바로 만들면서,
  이 게이트가 예전에 두던 "이미 런이 진행 중이면 지나간다" 예외가
  구멍이 됐다 — 튜토리얼을 마치지 않은 계정의 유일한 런은 언제나
  튜토리얼 런이라, 그 예외가 사실상 "튜토리얼 런 중엔 게이트를 끈다"는
  뜻이 되어 있었다(테스트로 실제 재현·확인). 그 예외를 뺐다.
- 가입 화면에 고정 홍보 그림(정적 에셋, Pillow 아님)을 붙일 자리를
  만들었다 — `assets/promo/join.png`. 실제 그림은 아직 없고(콘텐츠
  저작 단계 필요, A-1.4), 없으면 첨부 없이 화면이 그대로 나간다.

**A-1.2 허브 대시보드**

- `render_hub`를 다시 그렸다 — 대표 캐릭터 초상(지금은 항상 스타터,
  A-1.4), 닉네임, 코인/실버/카르타/와일드카드, 캐릭터 보유수/전체,
  파티·패시브 슬롯 해금 현황, 업적 진행률.
- 닉네임은 중앙봇 `GET /v1/users/{id}` 응답에서 코인 잔액과 같은 방식
  (`COIN_BALANCE_KEYS`)으로 흔한 키 이름을 순서대로 본다 — 이 저장소엔
  응답 스키마가 없어서다. 두 값 다 같은 호출 하나에서 나오도록 합쳤다.
  패시브 슬롯 상한은 전용 밸런싱 상수가 없어 연구 노드(§9.2) 중
  `passive_slot` 종류의 최댓값으로 계산한다.
- 빠른 실행 버튼 7개(시작/덱 관리/뽑기/캐릭터/연구/상점/업적) 추가.
  가입 버튼과 같은 이유로 custom_id에 user_id36을 담아 소유권을
  대조한다. 문서는 4+3 두 줄을 말하지만 실제 액션 로우는 5개씩 자동으로
  채워진다(§1.3.7) — 순서는 문서대로 지키고 정확한 줄 나눔은 사소한
  차이로 남겼다. 버튼은 텍스트 명령과 **같은 핸들러**를 부른다(문서
  원칙 그대로 — 지름길이지 별도 경로가 아니다).

**A-1.3 `!덱아웃 도움말`**

- 정적 텍스트(Pillow 아님)로 부속명령 9개를 안내한다. 튜토리얼 전에도
  쓸 수 있게 허용 목록에 넣었다 — 순수 정보라 게이트할 이유가 없다.

**테스트**: `tests/test_onboarding_hub.py` 신설(15개) — 소유권 대조
(가입·빠른 실행 버튼 둘 다), 가입 후 튜토리얼 직행, 경합 시 조용히
허브로, 닉네임 폴백, 빠른 실행이 텍스트 명령과 같은 화면을 내는지,
패시브 슬롯 상한 계산, 도움말 접근성, 그리고 **런이 있어도 강제
튜토리얼 게이트가 안 풀리는지**(고친 회귀). 기존 가입 관련 테스트
3개(`test_playthrough.py`, `test_render_and_api.py`, `test_surfaces.py`)
는 새 custom_id 형식·가입이 곧장 스레드를 여는 흐름에 맞춰 갱신했다.

검증: `python -m app.cli.bootstrap`, `python -m app.cli.check_content`,
`pytest -q` (전체 스위트) 모두 통과. 대시보드 렌더는 실제로 그려서 눈으로
확인.

## 2026-08-21

### 14:30 KST — §10.7 GitHub 콘텐츠 동기화 구현

설계 문서 v7 대조에서 나온 마지막 미구현 항목. 발행마다 콘텐츠를
`content-data/`에 git으로 미러링한다 — SQLite가 여전히 유일한 원본이고,
이건 사람이 읽을 수 있는 백업이자 엔티티별 변경 이력이다.

**문서 자체 모순 발견 및 오너 확인**: §10.6은 "발행마다 바뀌지 않은 행도
포함해 전부 다음 버전 번호로 복사한다"고 하는 반면, §10.7은 각 파일에
그 버전 번호(와 지문)를 적으라면서 "바뀐 엔티티만 변경된 파일로 잡혀
커밋된다"고 설명한다. 글자 그대로 구현하면 매 발행마다 모든 행의
`content_version_id`가 새 값으로 바뀌므로 사실상 전체 파일이 항상
바뀐 것으로 커밋돼, "N개 엔티티만 바뀌었다"는 설명 자체가 무의미해진다
(파일 하나에 `git log`를 걸어 그 엔티티만의 이력을 본다는 §10.7의 핵심
목적도 사라진다). 오너에게 물어 **실제 데이터가 바뀐 엔티티만 커밋**하는
쪽으로 확정했다 — 파일 안의 `content_version_id`는 "현재 발행 버전"이
아니라 "이 엔티티가 마지막으로 실제로 바뀐 버전"을 뜻하게 된다. 같은
이유로 버전 전체 지문(fingerprint)도 파일 안에는 넣지 않고 커밋
메시지에만 남긴다(§10.7이 "구현 세부사항"이라 허용한 자리).

- **스키마**: `content_sync_log` 테이블 추가(schema v6→7) — 버전별 커밋
  SHA·push 여부·실패 사유를 남겨 대시보드에 보여준다.
- **설정**: `content_repo_path`(비어 있으면 완전히 꺼짐)와
  `content_repo_push`(기본 꺼짐 — 자격 증명 없는 환경에서는 로컬 커밋만).
  이 저장소 자체를 대상으로 설정될 위험을 피하려고 **기본값이 꺼짐**이라,
  이 세션의 테스트나 평소 작업 중에는 전혀 동작하지 않는다.
- **`app/content/github_sync.py`**: `sync_publish()`가 §10.6
  `CONTENT_TABLES`의 모든 테이블·모든 행을 `content-data/<table>/<키>.json`
  으로 직렬화하고, 실제로 바뀐 파일만 골라 `git add`+`git commit`한다.
  `attempt_push()`는 실패해도 예외를 던지지 않는다(§10.7 Reliability —
  push 실패가 발행 자체를 막으면 안 된다). 동시 발행 직렬화는
  `threading.Lock`으로 — 이 프로세스가 그 repo_path의 유일한 쓰기자이므로
  §16.6이 말하는 단일 쓰기자 큐의 요구를 그대로 만족한다.
- **`versioning.publish()` 훅**: SQLite 커밋이 이미 끝난 뒤에만 동기화를
  시도하고, 실패는 로그만 남기고 삼킨다 — 발행 자체는 절대 막지 않는다.
- **되돌리기**: git 이력이 곧 엔티티 이력이라는 §10.7 설계를 그대로 써서
  `history_for`/`value_at`으로 과거 값을 읽고, 대시보드 `/admin/content/
  <table>/<key>/{history,revert-diff,revert}` 세 엔드포인트로 노출했다.
  되돌리기는 그 값을 초안에 얹고 §10.5 검증을 통과해야만 발행되며, 발행
  카운터를 되감지 않고 **새 버전**을 만든다(§10.6).
- **콘텐츠 편집 화면(§10.1)에 이력·되돌리기 UI 추가** — "이력 보기"를
  누르면 이 엔티티의 커밋 목록이 뜨고, 옛 시점의 "되돌리기"를 누르면
  **바뀌는 필드만** 현재값·되돌릴값을 나란히 보여준다(§10.7 되돌리기
  step 2 — "확인을 누르기 전까지는 아무것도 바뀌지 않는다"). 실제 서버를
  띄우고 Chromium으로 로그인 → 이력 보기 → 되돌리기 확인 → 확정까지
  전체 흐름을 직접 눌러 확인했다(스크린샷 확보). 동기화가 꺼져 있으면
  이 구역 자체가 화면에서 사라진다.
- **버전 목록 화면**: 동기화가 켜져 있을 때만 커밋 상태 열이 보인다.
- **테스트**: `tests/test_github_sync.py`(9개) + `tests/
  test_github_sync_admin.py`(8개) — 신규 파일 커밋, 안 바뀐 엔티티가 이전
  버전 번호를 유지하는지, 재동기화 시 커밋이 안 생기는지, git/push 실패가
  발행을 막지 않는지, 기본 설정(꺼짐)에서는 git을 아예 건드리지 않는지,
  대시보드를 통한 이력 조회·diff·되돌리기 전체 흐름(HTTP), 그리고 편집
  화면에 이력 구역이 켜짐/꺼짐에 따라 나타나고 사라지는지.

검증: `python -m app.cli.bootstrap`, `python -m app.cli.check_content`,
`pytest -q` (전체 스위트) 모두 통과.

### 13:05 KST — §5.3a 카드 도감 구현

오너가 설계 문서 v7 대조에서 남은 두 미구현 항목(카드 도감, §10.7 GitHub
콘텐츠 동기화) 중 카드 도감을 다음 작업으로 골랐다.

`!덱아웃 덱` 화면(런 안팎 어디서든)에 **[도감]** 버튼을 추가했다. 읽기
전용 화면으로, 뽑아 본 적 없는 행동 카드도 칸이 항상 있고 — 속성과 등급은
보이지만 이름·그림·종류(효과 계열을 드러내므로)는 가려진다.

- **`app/content/catalog.py`**: `compendium()` 신설 — 계정이 가졌든
  안 가졌든 행동 카드 전 칸을 돌려준다. 캐릭터는 뺐다 — §5.3a가 근거로
  삼는 §5.3 자체가 카드(행동 카드)의 영구 해금만 다루고, 캐릭터는
  §4/§5.2의 별도 성급·조각 화면(`characters_screen`) 몫이기 때문이다.
- **`app/render/panels.py`**: `render_card()`에 `masked` 매개변수 추가 —
  진짜 그림이 있어도 `AssetLibrary`를 아예 거치지 않고 별도로 가린
  칸(패널 + "?" 글자)을 그린다. "그림이 없어서 대체 실루엣을 쓰는 경우"
  (§11)와 "안 가져서 가리는 경우"가 화면에서 같아 보이면 안 되기 때문에,
  문서가 요구한 대로 별도 렌더 분기로 만들었다. 종류 배지가 비운 자리에
  속성 배지를 대신 넣었다 — 이 화면 전에는 카드에 속성을 글자로 보여주는
  자리가 없었다. `render_compendium()` 신설 — 가진 칸을 앞에, 그 안에서
  등급 높은 순으로 배열한다(처음에는 등급순으로만 정렬했다가, 화면 용량을
  넘는 콘텐츠에서 가진 카드가 전부 뒤로 밀려 화면 전체가 물음표만 보이는
  버그를 렌더 결과를 직접 눈으로 보고 발견해 고쳤다).
- **`app/api/visuals.py` / `app/api/handlers.py` / `app/api/hub.py`**:
  `visuals.compendium()`, `handlers.catalog_screen()` 신설. `[도감]`
  버튼은 `dko:hub:catalog` — 재화도 게이트도 없는 순수 읽기 전용이라
  `hub._dispatch`의 메시지+새로고침 모양을 타지 않고 완성된 화면을 그대로
  돌려준다. 카드가 하나도 없을 때의 빈 덱 화면에도, 런 안의 덱 화면에도
  버튼을 달았다 — 문서가 "런 안팎 어디서든"이라고 명시했기 때문이다.
- **테스트**: `tests/test_card_compendium.py` 신설, 11개 — 도감이 전
  칸을 돌려주는지, 캐릭터를 빼는지, 가려진 카드가 실제로 다르게 그려지고
  등급은 그대로 보이는지, 버튼이 빈 덱·일반 덱·런 중 덱 화면 모두에
  달리는지, 허브 라우팅이 실제로 화면에 닿는지.

검증: `python -m app.cli.bootstrap`, `python -m app.cli.check_content`,
`pytest -q` (전체 스위트) 모두 통과. 렌더 결과를 실제 PNG로 저장해
육안으로도 확인.

### 11:20 KST — §2.13 반응형 능력(반격/카운터) + §10.4.3 크리티컬 구현

오너 확인: §4.6.5는 기존 가입 버튼 방식을 그대로 유지(문서 대신 코드가
기준), §2.13(반응형 능력)과 크리티컬 시스템 둘 다 구현하기로 결정.

문서 v7이 이번 세션에 새로 추가한 §2.13을 처음부터 끝까지 구현했다 —
유닛에 종속되지 않는(unit-agnostic) 콘텐츠 타입으로, 캐릭터든 적이든
`on_damage_taken` 트리거를 가진 반응형 능력을 가질 수 있다.

- **스키마**: `reactive_abilities` 테이블 추가 (`schema_version` 5 → 6).
  `content_version_id, reactive_ability_id` 두 부분 키(§10.6)를 따르고,
  §10.6 발행 복사·지문·대시보드 CRUD가 자동으로 붙도록
  `versioning.CONTENT_TABLES`에 등록했다 — 별도 대시보드 라우트를 새로
  만들 필요가 없었다.
- **엔진 훅**: `effects._land()`(모든 `deal_damage`/`deal_flat_damage`가
  최종 피해를 반영하는 유일한 지점, §2.11 P8/E3)에서 곧바로
  `engine.reactive.fire_on_damage_taken()`을 호출한다. 사망 시 무효
  처리, 조건 평가(§10.4.5, 새 `random_chance` 연산자 포함), 효과 목록
  실행, 반격 자신의 사망 체크까지 문서 순서 그대로 따른다.
- **무한 루프 방지**: 서로를 반격하는 콘텐츠 조합이 턴을 붙들지 않도록
  `reactive_ability_max_depth`(기본 4, `config/01_전투.toml`, §15
  하드코딩 금지 원칙에 따라 설정값)로 재귀 깊이를 제한했다. 문서에
  명시되지 않은 순수 엔진 안전장치라는 점을 주석에 남겼다.
- **RNG 저널링**: `random_chance` 조건은 §16.4 저널을 통해서만 굴린다 —
  같은 (전투, 라운드) 안에서 서로 다른 시점에 굴리는 롤이 같은 op_key로
  충돌하지 않도록, 실제로 굴림이 필요한 순간에만 기존 저널 행 수를 세어
  다음 순번을 매기는 방식을 썼다(전투 한 판 내 처리는 항상 순차적이라
  안전하다 — §16.7 CAS가 보장하는 전제).
- **검증(§10.5)**: 소유자(owner_content_type/owner_id)가 실제로
  존재하는지, `trigger_event`가 지원되는 값인지, 조건 연산자가
  §10.4.5 닫힌 집합에 속하는지, 효과가 PURE_SYNCHRONOUS ∩ BATTLE_SAFE인지
  (§10.4.1a — 카드/패시브와 같은 제약, 전투 턴 안에서 즉시 실행되므로),
  그리고 상태 효과의 §2.5.1a 풀 소속(캐릭터 소유는 player 풀 → enemy_only
  금지, 적 소유는 enemy 풀 → player_only 금지)까지 검사한다.
- **시드 콘텐츠**: 두 예시를 넣어 두 소유 타입 모두 실제로 동작함을
  보였다 — `char_terradon`의 "돌벽의 반격"(HP 50% 이하일 때, 조건부)과
  `enemy_w2_가시덩굴`의 "가시 반격"(무조건).
- **테스트**: `tests/test_reactive_abilities.py` 신설, 10개 —
  양쪽 소유 타입의 발동, 조건 게이팅, 사망 시 무효, 재귀 깊이 상한(정확히
  `depth_cap + 1`개의 피해 이벤트로 멈추는지까지), `random_chance`의
  저널링·재현, 그리고 §10.5 검증 실패 케이스 세 가지.

**§10.4.3 크리티컬(`crit_chance`/`crit_multiplier`)** 도 같은 작업에서
같이 구현했다 — `deal_damage`/`deal_flat_damage` 둘 다에 붙는 선택 파라미터.

- **연산자 스키마**: 두 연산자 모두 `crit_chance`(기본 0)·`crit_multiplier`
  (선택)를 받는다. §10.5가 `crit_chance`는 0..1, `crit_multiplier`는
  0보다 큰 값인지 검사한다.
- **판정**: 해상 시점에 저널링된(§16.4) `random_chance(crit_chance)`를
  굴린다 — `crit_chance`나 `crit_multiplier` 중 하나라도 없으면 굴리지
  않고 저널 행도 남기지 않는다(대부분의 콘텐츠는 크리티컬을 안 쓰므로).
  op_key는 반응형 능력의 `counter:` 와 같은 지연 순번 할당 방식을
  `crit:` 네임스페이스로 공유한다(`rng.next_journaled_seq` 로 공통화).
- **적용**: 성공하면 `crit_multiplier`가 원래의 `multiplier`/`amount`
  스케일링을 **대체**한다(곱해서 누적하지 않음) — `deal_damage`는
  `multiplier` 자리에 `crit_multiplier`를 그대로 쓰고, `deal_flat_damage`는
  정수 `amount`에 스케일 개념이 없어 `amount × crit_multiplier`로 해석했다
  (문서 문구가 flat 쪽에는 다소 모호해 이 해석을 코드 주석에 남겼다).
  캐릭터 스탯이 아니라 카드/행동 단위로만 존재하며, §15.1 스탯 파이프라인에
  새 항목을 추가하지 않는다.
- **시드 콘텐츠**: `card_풍_질풍`(플레이어 카드)과 `act_강타`(적 행동)
  양쪽에 걸어 두 경로 모두 실제로 동작함을 보였다.
- **테스트**: `tests/test_crit.py` 신설, 8개 — 강제 크리티컬이 대체이지
  누적이 아님을 배율 비교로 증명, flat 피해 스케일링, 저널링 여부,
  `crit_chance=0`일 때 롤 자체가 없는지, 그리고 §10.5 검증 3가지.

검증: `python -m app.cli.bootstrap`, `python -m app.cli.check_content`,
`pytest -q` (전체 스위트) 모두 통과.

### 09:40 KST — 설계 문서 v7 반영: 시작 캐릭터 이름·런 화폐 표시 이름 확정

오너가 업로드한 Deckout Design Doc v7을 처음부터 끝까지 읽고 현재 구현과
대조했습니다. 그중 §13.1이 이번 세션에 해결됐다고 밝힌 두 개의 🔴 PENDING
표시 항목(내부 식별자는 그대로 두고 표시 이름만 정해지지 않았던 것들)을
반영했습니다.

- **P-2**: 시작 캐릭터(`starter_001`)의 표시 이름이 `견습 모험가`라는
  임시값이었는데, `루야`로 확정됐습니다. `app/content/seed.py`의 캐릭터
  시드 행을 고쳤습니다.
- **P-3**: 런 스코프 화폐(`runs.run_currency`)의 화면 표시 이름이
  `탐험 자금`이었는데, `실버`로 확정됐습니다. 플레이어가 실제로 보는
  문구만 고쳤습니다 — 상점 화면 제목 표시(`app/render/panels.py`
  `render_shop`), 구매 완료·전투 승리 메시지(`app/api/handlers.py`),
  상점 품목 셀렉트 설명(`app/api/controls.py`). `runs.run_currency` 컬럼명,
  operator 파라미터 `"run_currency"`, 그리고 코드 주석·독스트링 속 개념
  설명은 내부 식별자이므로 그대로 뒀습니다.

**대조 중 발견한, 아직 손대지 않은 더 큰 차이들** (오너 우선순위 확인 필요):

- §4.6.5: 문서는 여전히 "미가입 사용자가 아무 명령이나 치면 조용히 계정
  자동 생성"으로 서술돼 있는데, 이전에 오너 지시로 명시적 가입 버튼 +
  강제 튜토리얼로 이미 바꿔 놨습니다(08-19 기록 참고). 문서와 코드 중
  어느 쪽을 기준으로 삼을지는 여전히 미확정입니다.
- §10.7 GitHub 콘텐츠 동기화(발행마다 `content-data/`에 git 커밋) — 미구현.
- §2.13 반응형 능력(반격/카운터, 피해 연산자 뒤에 새 트리거 지점 추가) —
  미구현.
- `crit_chance`/`crit_multiplier` 치명타 시스템(§10.4.3) — 미구현.
- §5.3a 카드 도감 화면 — 미구현(`app/content/catalog.py`에 주석으로만
  언급되어 있음).

검증: `pytest -q` 결과 748개 테스트 통과.

## 2026-08-19

### 10:10 KST — 명시적 가입 단계, 그리고 튜토리얼을 마치기 전엔 다른 진행을 막음

오너 지시로 두 가지를 바꿨습니다.

**1. 가입을 명시적인 단계로 만들었습니다.** §4.6.5는 지금까지 알 수 없는
사용자가 아무 `!덱아웃` 명령이나 치면 그 자리에서 조용히 계정을 만들고
바로 요청한 화면으로 넘겼습니다 — "가입"이라는 순간이 플레이어에게 전혀
보이지 않았다는 뜻입니다. 이제 계정이 없는 사용자의 첫 명령은 무엇을
쳤든 "가입하기" 버튼 하나만 있는 환영 화면을 돌려주고, 그 버튼을 눌러야
비로소 계정(§4.6.5의 계정 행·시작 캐릭터·18장 스타터 덱·초기 재화)이
만들어집니다. 런 밖 화면들과 같은 이유로(§19.2의 `custom_id`는 run_id를
요구하는데 가입 전에는 run_id 자체가 없습니다) `dko:reg:` 전용 접두사를
새로 뒀습니다.

**주의:** 이건 §4.6.5가 실제로 문서화한 동작(조용한 자동 생성)과 다른
방향입니다. 설계 문서를 임의로 바꾼 게 아니라 오너가 직접 이 방향을
지시해서 진행했습니다 — 나중에 §4.6.5 문서 자체를 갱신할지는 오너
확인이 필요합니다.

**2. 튜토리얼을 마치기 전에는 다른 진행을 열지 않습니다("강제
튜토리얼").** 계정은 있지만 `tutorial_completed_at`이 비어 있고 진행 중인
런도 없는 사용자가 `뽑기`/`상점`/`캐릭터`/`장비`/`연구`/`업적`/`덱`/`패시브`
중 아무거나 치면 "튜토리얼을 먼저 완료해 주세요"로 막습니다. 허브(빈
명령)와 `시작`, `포기`만 그전에도 열려 있습니다 — 그래야 튜토리얼 자체를
진행하고 막히면 포기할 길이 있습니다. 튜토리얼을 마쳤거나 이미 런이
진행 중이면 그대로 지나갑니다.

`app/api/handlers.py`의 `handle_message`에 두 검사를 추가하고,
`REGISTER_PREFIX` 처리를 `handle_interaction`에 배선했습니다. 테스트
쪽에서는 `tests/test_playthrough.py`의 공용 `Session.command()`가 가입
프롬프트를 만나면 자동으로 눌러 주도록 고쳤습니다(그 픽스처를 쓰는
기존 테스트 대부분은 `user_id` 픽스처가 `create_account`를 미리 불러
계정을 만들어 두므로 이 경로 자체를 타지 않습니다 — 그래서 계정이 아예
없는 사용자로 시작하는 새 테스트 두 개를 따로 추가했습니다).

검증: `pytest -q` 전체 스위트 통과(신규 2개 — 가입 전에는 계정이 안
생기고 버튼을 눌러야 생기는지, 갓 가입한 계정이 튜토리얼 전에 다른
명령을 못 여는지).

## 2026-08-17

### 19:30 KST — 세계 1~4에 적 8마리 추가 (변칙 행동 포함)

오너 요청으로 적 콘텐츠를 늘렸습니다. 능력치·행동은 설계 문서(§15 등)에
근거가 없는 값이라 §0.1 기준으로는 발명 대상이라, 진행 전에 오너에게
확인했습니다 — 기존 세계별 스탯 밴드에 맞춰 직접 지어도 되고, 단순
스탯 반복이 아닌 변칙(특수) 적을 넣어도 된다는 답을 받고 진행했습니다.
새 상태이상·새 연산자는 만들지 않고 §2.5.1a의 기존 10개 상태와 기존
연산자 조합만 썼습니다 — 상태를 늘리면 §10.5 scope 검증이 함께 늘어나기
때문입니다.

세계마다 둘씩:

- **1세계(고블린 굴)** `enemy_w1_늑대조련사`(3턴마다 아군에게 공격력 증가
  부여), `enemy_w1_돌격병`(체력 50% 이하가 되면 필사의 공격력 강화)
- **2세계(잠긴 늪)** `enemy_w2_늪치유사`(체력 낮은 아군을 25% 회복),
  `enemy_w2_가시덩굴`(같은 편이 하나만 남으면 1라운드 무적)
- **3세계(바람의 절벽)** `enemy_w3_폭풍소환사`(엘리트 — 3라운드째부터
  하피 소환), `enemy_w3_돌풍매`(2라운드째부터 매 3턴마다 속도 상승)
- **4세계(심연의 문)** `enemy_w4_재생하는망자`(체력 50% 이하에서 자가
  회복 20%), `enemy_w4_무적파수꾼`(엘리트 — 3턴마다 한 번 1라운드 무적)

이 능력을 실제로 실행하려고 `enemy_actions`에 `act_아군투지`/
`act_필사반격`/`act_아군치유`/`act_무적화`/`act_소환_하피`/`act_가속`/
`act_자가치유` 일곱 개를 새로 추가했고, 각 세계에 새 조우
(`enc_w1_normal_3`, `enc_w2_normal_3`, `enc_w3_normal_3`,
`enc_w3_elite_2`, `enc_w4_normal_3`, `enc_w4_elite_2`)를 만들어 실제
맵에서 뽑히도록 연결했습니다.

작업 중 기존 버그 하나를 우연히 발견했습니다: `heal` 연산자의 비율
모드 이름이 `"percent"`가 아니라 `"percent_max_hp"`였는데, 처음에는
잘못된 이름으로 써서 §10.5 검증에서 바로 걸렸습니다 — 발행 전에
잡혔으니 실제로는 문제가 되지 않았습니다.

검증: `python -m app.cli.bootstrap --db deckout.db` 로 새 콘텐츠 버전
발행, `python -m app.cli.check_content --db deckout.db` §10.5 통과,
`python -m app.cli.make_art --kind enemy` 로 새 적 8장 자리표시 그림
생성, 새 적을 실제 전투 화면(`render_battle_screen`)에 그려 눈으로
확인. `pytest -q` 전체 스위트 통과.

## 2026-08-14

### 16:12 KST — 무적과 스탯 변화가 계산에는 반영되면서 화면엔 없었다

`app/engine/timed_effects.py` (§2.5.3, 라운드 한정 무적·스탯 변화)는 전투
계산에는 완전히 연결돼 있었습니다 — `is_invulnerable`이 대미지를 실제로
막고, `stat_delta`가 `effective_atk`/`effective_def`/`effective_spd`에
그대로 더해집니다. 하지만 이 모듈에 있는 `active_for_unit`을 부르는 곳이
어디에도 없었습니다: 화면을 만드는 `visuals._unit_view`는 지속 상태이상
(`statuses`)만 읽었지 이 테이블은 조회조차 하지 않았습니다. 그 결과
플레이어는 대미지가 왜 0인지(무적), 공격력이 왜 갑자기 달라졌는지
(스탯 변화) 화면에서 알 방법이 없었습니다 — 계산은 맞는데 이유가 안
보이는, 이번 세션에서 반복된 것과 같은 모양의 구멍입니다.

같은 자리를 보다가 하나 더 나왔습니다: **적 패널은 상태이상을 한 번도
그린 적이 없었습니다.** 아군 패널은 처음부터 `statuses`를 표시했는데
적 패널(`render_enemy_panel`)은 그 줄 자체가 없었습니다 — 화상을 건
적이 그림에서는 멀쩡해 보였다는 뜻입니다.

`_unit_view`에 `timed_effects`를 추가하고, `panels.py`에 `_status_line`
헬퍼를 만들어 지속 상태이상과 라운드 한정 효과를 한 줄로 합쳐 아군·적
패널 둘 다에서 보여주도록 고쳤습니다.

검증: `pytest -q` 결과 741개 테스트 통과(신규 3개 — DB에서 화면 dict까지
실제로 실리는지, 라벨 문자열이 맞는지, 적 패널이 렌더 실패 없이 그리는지).
아군·적 패널을 실제로 PNG로 찍어 무적·상태이상 표시를 눈으로 확인했습니다.

### 15:32 KST — 글자만 있던 화면 일곱 곳에 그림을 붙였다

허브·캐릭터·장비·연구·업적·허브상점·(런 중) 덱 — 일곱 화면이 목록을 전부
`**굵은 글자**`와 줄바꿈으로만 보여주고 있었습니다. 전투·지도·상점(런
중)·뽑기·준비·소장 화면은 이미 §11 Pillow 렌더가 붙어 있었는데, 이 일곱
곳만 처음부터 텍스트 전용이었습니다 — 그릴 만한 카드·지도 같은 '내용물'이
없다는 이유로 후순위였던 것으로 보입니다.

`app/render/panels.py`에 `render_hub`/`render_characters`/`render_equipment`/
`render_hub_shop`/`render_research`/`render_achievements`/`render_run_deck`
일곱 함수를 새로 추가하고, `app/api/visuals.py`에 얇은 래퍼를 하나씩
붙였습니다. 텍스트 줄은 전부 그대로 남겨 두었습니다 — 그림이 실패해도
화면은 글자만으로 나가야 한다는 §11의 약속은 이 화면들도 같습니다.

의도적으로 건드리지 않은 곳: 준비 화면 [1]-[2]단계(월드·파티 선택)와
보상/이벤트 선택 화면은 그릴 내용이 드롭다운 선택지 자체뿐이라 원래부터
그림이 없습니다 — 이건 빠뜨린 것이 아니라 기존 설계와 같은 모양입니다.

검증: `pytest -q` 결과 738개 테스트 통과(신규 8개 — 렌더 함수 자체와, 핸들러
응답에 실제로 `attachments`가 실리는지 둘 다 확인). 새 렌더 함수 일곱 개와
전투 화면(손패 포함, 2장 한도)을 실제로 PNG로 찍어 확인했습니다.

### 15:20 KST — 중앙봇 연동 계약 위반 넷, 그리고 실행되지 않던 화면 복구 계획

실제 운영 중 신고가 들어왔습니다: `!덱아웃 시작` 이 "처리 중 문제가
발생했습니다. 잠시 후 다시 시도해주세요" 로 실패하고, 그 뒤로는 계정이
"이미 진행 중인 런이 있습니다" 로 막혔습니다. 함께 전달받은 기술 분석을
코드로 하나하나 확인해 보니 전부 사실이었습니다.

**1. 컴포넌트를 평면 목록·문자열 타입으로 보내고 있었습니다.** 실제 배포된
Central Bot 의 Component Contract 2.0 은 명시적 action row
(`{"type": 1, "components": [...]}`) 와 숫자 컴포넌트 타입(버튼 2, 문자열
선택 3)을 요구하는데, `controls.py`/`handlers.py`/`hub.py`/`screens.py` 네
곳 모두 사람이 읽기 쉬운 `{"type": "button"}` 평면 목록을 그대로 응답에
담고 있었습니다. 지도·전투·보상·상점·이벤트를 포함해 **컴포넌트가 있는
모든 응답**이 여기 해당했고, `!덱아웃 시작` 의 마지막 단계도 컴포넌트라
바로 이 형식 오류로 거절됐습니다. 네 파일을 전부 고치는 대신
`CentralClient.to_action_rows()` 하나를 만들어 응답이 실제로 나가는 두
경계(직접 HTTP 응답 몸통인 `/event`, 그리고 `create_thread`/`edit_message`)
에서만 변환하도록 했습니다 — 컴포넌트를 만드는 코드는 그대로 두고, 나가는
자리에서만 계약에 맞춥니다.

**2. `/v1/currency/add` 응답 스키마를 잘못 읽고 있었습니다.**
`CentralClient.currency_add`/`currency_deduct` 는 `applied_delta`/`applied`/
`balance` 키만 읽었는데, 실제 Central 의 `ChangeResponse` 는 `amount`/
`value_after` 를 돌려줍니다. 그 결과 실제 +500 출석 코인 지급이 Central
쪽에서는 정상 커밋됐는데도 Deckout 은 적용액을 0으로 읽어
`operator_required` 로 빠졌고, 로컬 출석 기록과 보상(카르타 등)이 반영되지
않았습니다. 실제 스키마(`amount`/`value_after`)를 우선 읽고, 예전에
가정했던 키들은 방어적 대체로만 남겼습니다 — `THREAD_ID_KEYS` 와 같은
원칙입니다.

**3. 전투 화면이 첨부 두 장 한도를 넘기고 있었습니다.** 아군·적 패널
두 장에 손패가 있으면 세 번째 `deckout_hand.png` 를 더 붙였는데, 실제
전투는 항상 손패가 있으므로 **살아 있는 모든 전투 턴**이 이 한도를
넘겨 거절될 상황이었습니다. 손패를 별도 첨부로 보내는 대신 아군 패널
아래에 이어 붙여 한 장으로 합쳐, 정보를 잃지 않으면서 항상 두 장을
유지하도록 고쳤습니다.

**4. 첨부에 `content_type` 이 없었습니다.** `visuals.attach()` 가
`filename`/`data_b64` 만 보내고 있어서, `content_type: "image/png"` 를
추가했습니다.

**그리고 하나 더 — 로컬 상태는 앞섰는데 화면 전달이 실패한 런.**
`recover_runs()` 는 처음부터 이런 런을 `resume_map`/`rerender_battle`/
`rerender_pending_choice` 로 분류하고 있었지만, 그 계획을 실행하는 코드가
없어서 계획으로만 남아 있었습니다(만료 정산과 정산 재개만 실제로
실행되고 있었습니다 — 14:20 항목 참고). 노드 해결이 전투로 끝난 직후
응답 전달이 실패하면, 런은 로컬로 `battle` 상태인데 스레드에는 지도가
그대로 남고 버튼도 맞지 않는 채로 방치될 수 있었습니다 — 신고된 증상과
정확히 같은 모양입니다. `surfaces.rerender_stale_screen()` 을 새로 만들어
이미 커밋된 상태만으로 글자·버튼을 다시 맞춰 내보내도록 기동 복구에
연결했습니다. 게임 로직은 다시 실행하지 않습니다 — 노드를 다시 해결하거나
보상을 다시 굴리면 이미 벌어진 일을 되풀이하는 셈이 되기 때문입니다.
`edit_message` 는 그림을 새로 보낼 수 없어(§1.3.6, `attachment_policy` 는
`preserve` 뿐) 글자와 버튼만 고치고, 그림은 다음 실제 조작에서 자연히
새로 그려지도록 남겨 뒀습니다. 안전하게 재구성하려면 로직 재실행이
필요한 두 경우(노드 해결 도중, 중첩된 연산자 정지 화면)는 건드리지
않고 남겨 뒀습니다 — 잘못 재구성하는 것보다는 낫습니다.

**`operator_required` 로 갇힌 트랜잭션은 파싱을 고쳐도 저절로 풀리지
않습니다.** 이 상태는 §17.3 규칙 8에 따라 자동 복구 대상에서 의도적으로
제외되어 있어서(`resume_pending` 이 건드리지 않습니다), 위 2번 버그로
실제 지급이 있었는데도 갇힌 트랜잭션은 코드를 고친 뒤에도 그대로 갇혀
있습니다. `transactions.reconcile_operator_required()` 와
`python -m app.cli.reconcile_transactions` 를 새로 만들었습니다 — **같은
멱등성 키**로 `currency_add` 를 다시 불러 Central 이 이미 저장해 둔 결과를
재생시킬 뿐, 새 금액을 요청하지 않습니다. 재조회해도 여전히 안 맞으면
진짜 이상 현상이므로 자동으로 풀지 않고 그대로 둡니다. 기동 시 자동
실행되지 않는, 운영자가 손으로 트리거하는 도구입니다 — 상태 이름 그대로
운영자의 몫입니다.

검증: `pytest -q` 결과 735개 테스트 통과(신규 23개, `tests/test_recovery.py`).
`CentralClient` 의 실제 HTTP/JSON 파싱 경로를 직접 때리는 테스트가 이번에
처음 생겼습니다 — 지금까지는 전부 손으로 만든 Fake 가 파싱을 건너뛰고
있어서 이 스키마 버그가 조용히 배포됐었습니다.

### 14:55 KST — 문서만 약속하고 없던 검사 둘, 그리고 제가 만든 버그 하나

`config/01_전투.toml` 은 `card_cost_min`/`card_cost_max` 를 "이 범위를 벗어난
카드는 §10.5에서 저장이 거부됩니다" 라고 설명합니다. **그 검사는 존재하지
않았습니다.** 확인해 보니 그 틈으로 실제로 발행되어 있던 카드가 있었습니다 —
`card_무_재정비`, 이번 세션에서 콘텐츠를 늘리며 제가 직접 만든 카드입니다.
비용을 0으로 적어 두고 정작 config 가 정한 최솟값(1)을 어겼는데, 검사가 없어서
아무도(저를 포함해) 몰랐습니다. 비용을 `card_cost_min` 으로 고치고(하드코딩하지
않고 설정에서 읽습니다), §10.5에 범위 검사를 추가했습니다.

`research_nodes.effect_json` 도 전혀 검사되지 않고 있었습니다. 두 가지가
조용히 가능했습니다:

- **오타 난 `kind` 를 쓴 연구 노드**를 발행하면 `_apply_research` 가 어느
  `elif` 에도 걸리지 않습니다 — **와일드카드와 코인만 나가고 아무 효과도
  없이 끝납니다.** `offer_reward` 가 비어 있던 것과 정확히 같은 모양의
  버그입니다.
- `party_slot` 값에 상한이 없어서, `max_party_slots`(정확히 이 용도로
  선언되어 있었지만 아무도 읽지 않던 설정)보다 큰 값을 그대로 지급할 수
  있었습니다. 덱 구성 화면과 인코운터 다운스케일(§15.1)이 실제로 지원하는
  파티 인원을 전제로 하므로, 그 값을 넘기면 화면 밖의 자리가 생깁니다.

§10.5에 `effect_json` 형식·`kind` 화이트리스트·`party_slot` 상한 검사를
추가했고, `_apply_research` 에도 알려지지 않은 `kind` 를 조용히 넘기는 대신
실패하도록 방어선을 하나 더 두었습니다 — §10.4.1a의 미구현 연산자 처리와
같은 원칙입니다: 검증을 뚫고 들어온 콘텐츠라도 재화만 받고 끝나서는 안 됩니다.

검증: `pytest -q` 결과 720개 테스트 통과(신규 9개). 기존 시드 콘텐츠 전체가
새 검사를 그대로 통과하는지, 그리고 실행 시점 방어가 실제로 예외를 내는지
모두 테스트로 확인했습니다.

### 14:20 KST — 기동 복구 계획을 실제로 실행한다

`recover_runs` 는 비종료 런을 훑어 무엇이 필요한지 분류해 돌려줍니다. 그런데
그 계획 중 **실제로 실행되는 것은 만료 정산 하나뿐** 이었고, 나머지는
`state["recovery_plan"]` 에 담겨 아무도 읽지 않았습니다. §16.3 만료가 같은
이유로 발동하지 않던 것과 똑같은 모양입니다.

나머지가 전부 문제인 것은 아닙니다. 전투나 상점 화면을 다시 그리는 일은 런이
그대로 살아 있고 `custom_id` 의 세대·리비전도 그대로라, 플레이어가 이어서
누르면 됩니다. 문제는 **플레이어가 스스로 풀 수 없는 둘** 입니다.

**정산 도중에 죽은 런.** `run_settlement` 은 활성 상태라 §16.3의 계정당 하나
규칙으로 계정을 붙들고 있는데, 정산 화면에는 누를 것이 없습니다. 플레이어가
할 수 있는 일이 아무것도 없고 30분 만료를 기다리는 수밖에 없었습니다. 이제
기동할 때 이어서 끝냅니다 — 정산 단계는 receipt(§17.6)와 저널 키(§16.4)로
멱등하므로 어디서 멈췄든 다시 들어가 마칠 수 있고, 기동이 두 번 일어나도
보상이 두 번 나가지 않는 것을 테스트로 못 박았습니다.

**화면을 얻지 못한 채 준비 상태로 남은 런.** `surfaces.retry_surface` 를
더했습니다. §16.8이 요구하는 대로 **같은 `surface_generation`** 으로 다시
만듭니다 — 세대를 올리는 것은 스레드가 실제로 사라졌을 때이고, 같은 세대의
생성은 멱등해서 실제로는 열렸는데 결과를 못 받은 경우에도 스레드가 둘이
되지 않습니다. 실패하면 준비 상태 그대로 두어 다음 기동이 다시 시도합니다.

둘 다 하나가 실패해도 나머지를 계속 처리합니다. 복구에 걸려 서비스가 아예
뜨지 못하면 막힌 계정을 풀어 줄 방법마저 사라지기 때문입니다.

### 13:50 KST — 테스트용 압축본과, 거기서 드러난 흔들리는 테스트

압축을 **다른 디렉터리에 풀어서** bootstrap → 검증 → 전체 테스트까지 실제로
돌려 보았습니다. 거기서 제 테스트 하나가 흔들리는 것이 드러났습니다.

`test_clearing_the_tutorial_opens_the_main_campaign` 이 지도를 눌러 보스까지
가는 방식이었는데, `pytest-randomly` 가 시드를 바꾸면 정해진 횟수 안에 보스에
닿지 못하는 경우가 있었습니다. **단독으로는 늘 통과하고 전체에서만 가끔
실패하는** — 제일 나쁜 종류입니다.

보스까지 눌러 가는 길은 옆 테스트가 이미 확인하고 있고 이 테스트의 주제는
**클리어 이후에 무슨 일이 일어나는가** 이므로, 보스 칸을 바로 열고 끝내도록
바꿨습니다. 지도 걸음의 운에 좌우되지 않습니다.

`테스트하기.md` 를 더했습니다. 설치·발행·테스트·서비스 기동·대시보드·수치
조정·그림 넣기까지 한국어 순서로 적었고, 환경 변수 두 개가 없으면 서비스가
뜨지 않는 이유도 함께 적었습니다.

검증: `pytest -q` 결과 703개 통과. 압축본을 푼 자리에서도 같은 명령이 그대로
돌아가는 것을 확인했습니다.

### 13:15 KST — 본편 런도 화면으로 돌려 보기, 그리고 지도가 거짓말하던 것

튜토리얼은 준비 화면을 전부 건너뜁니다(월드도 파티도 정해져 있습니다). 그래서
§16.2.2의 [1]~[6]과 파티 2명 전투, 자리를 고르는 보상 수령은 **본편에서만**
확인할 수 있습니다. 드라이버를 졸업한 계정으로도 돌렸습니다.

준비 화면과 본편 전투는 그대로 통과했습니다. 대신 다른 것이 나왔습니다.

**지도가 갈 수 있는 칸을 표시하지 않았습니다.** `render_map` 은 `available`
집합을 받아 갈 수 있는 칸을 밝게 그리도록 되어 있는데, `visuals.game_map` 이
그것을 **한 번도 넘기지 않았습니다.** 넘기지 않으면 기본값이 빈 집합이라
**현재 칸을 뺀 모든 칸이 "지나온 칸" 색**으로 그려집니다 — 버튼은 갈 곳을
알려 주는데 그림은 지도 전체가 이미 끝난 것처럼 보였습니다.

**`run_nodes.state` 는 아무도 갱신하지 않았습니다.** 만들 때 `available` 로
한 번 적히고 그걸로 끝이라, 지나온 칸과 아직 닿지 않은 칸이 구별되지
않았습니다. 칸에 들어설 때 `visited` 로 표시하고, 지도가 네 가지(지금 선 칸 ·
갈 수 있는 칸 · 지나온 칸 · 아직 닿지 않은 칸)를 다르게 그립니다.

**자리를 지킬 그림의 이름 띠가 배경에서 간판만 해졌습니다.** 띠 높이를 비율로만
정해 두어서, 560픽셀짜리 월드 배경에서는 글자가 40픽셀이 되어 그 위에 그려지는
보스 칸을 덮었습니다. 띠와 글자에 절대 상한을 두었습니다.

드라이버 쪽도 하나 고쳤습니다. `/event` 는 명령이든 컴포넌트든 **모든** 응답에
대해 스레드 요청을 실행하는데 드라이버는 명령에만 했습니다. 준비 화면의 마지막
단계가 컴포넌트라, 본편 런이 스레드 없이 만들어진 것처럼 보였습니다 — 서비스가
아니라 드라이버의 구멍이었습니다.

### 12:30 KST — 런 한 판을 끝까지 눌러 보기, 그리고 거기서 나온 결함 다섯

`tests/test_playthrough.py` 는 테스트이자 탐침입니다. 이번 작업에서 반복해 나온
결함은 전부 같은 모양이었습니다 — 엔진은 완성인데 그 앞의 한 층이 비어 있고,
테스트는 그 층을 건너뛰고 엔진을 직접 불러 통과합니다. 그래서 이 파일은
`custom_id` 를 **합성하지 않습니다.** 화면이 돌려준 컴포넌트에 들어 있는 것만
누르고, 런이 끝날 때까지 반복합니다. 중간에 누를 것이 없어지면 그 자리가 곧
구멍입니다.

돌려 보자마자 다섯 개가 나왔습니다.

1. **패배한 플레이어에게 "승리!" 라고 말했습니다.** `_conclusion_summary` 가
   튜토리얼 재도전을 `screen == "node_resolution"` 으로 판별했는데, 재도전이
   돌려주는 것은 다시 연 화면(대개 `battle`)이라 그 조건은 한 번도 맞지
   않았습니다.

2. **튜토리얼 재도전 전투가 아무에게도 몰려지지 않았습니다.** 새로 열린 전투는
   대개 적이 먼저 움직이는데(§2.8.6), 첫 플레이어 결정까지 몰아 주지 않으면
   적이 턴을 잡은 채로 멈추고 화면에 낼 수 있는 카드가 없어집니다 — 런이 그대로
   굳습니다. `_drive_battle` 안에서 이어 몰아 주도록 고쳐, 세 호출부가 모두
   같은 처리를 받습니다.

3. **결론이 연 화면 대신 방금 끝난 전투를 그렸습니다.** 전투가 끝나면
   `conclude_battle` 이 다음 화면(지도·정산·재도전 전투·다시 열린 이벤트)을
   열어 주는데, 화면과 그림은 계속 끝난 전투를 가리켰습니다. 그 위에는 누를
   것이 나오지 않아 역시 런이 굳습니다. `_effective_screen` 하나로 정리했습니다.

4. **선택으로 산 상점 물건이 ValueError 로 터졌습니다.** 진열이 4~6줄이라
   상점은 선택 컴포넌트를 쓰는데 `_on_shop_buy` 만 `values` 를 보지 않고
   payload 만 읽었습니다. 카드 선택·보상 수령은 이미 둘 다 받고 있었습니다.
   터지면 이벤트가 기록되지 않아 중앙봇이 같은 조작을 무한히 재전송합니다.

5. **살 수 없는 물건이 계속 목록에 남았습니다.** 빈털터리 플레이어에게 눌러도
   늘 거절되는 목록만 남습니다. 탐험 자금은 **로컬 재화** 라 여기서 걸러도
   틀릴 일이 없습니다 — 코인과 달리 다른 미니게임이 중간에 가져갈 수 없으므로
   §17.3이 코인 잔액으로 버튼을 막지 말라고 한 이유가 여기엔 없습니다.

고친 뒤 튜토리얼 한 판이 `!덱아웃` 부터 정산까지 실제로 돌아갑니다.

**밸런싱 관찰 (수치는 건드리지 않았습니다).** 자동으로 플레이해 본 결과
튜토리얼 보스에서 **52번째 시도에 처음 이겼습니다.** 매 시도가 10~12라운드까지
가는 접전이라 승패가 사실상 운에 달려 있습니다. §3.4.1대로 튜토리얼 패배는
비용이 없어 계속 도전할 수 있지만, 처음 하는 사람이 51번을 지고 나서야 넘어가는
것은 의도한 난이도가 아닐 수 있습니다. §15 수치는 오너 결정 사항이라 그대로
두었고, `config/01_전투.toml` 의 `enemy_bands` 와 시드의 튜토리얼 보스 능력치가
조정 지점입니다.

검증: `pytest -q` 결과 699개 테스트 통과(신규 4개). 이 테스트는 예산만큼 누른
뒤 끝나지 않으면 `포기` 로 마무리합니다 —
확인하려는 것이 "이길 수 있는가" 가 아니라 **"어느 화면에서도 막히지 않는가"**
이고, 승패는 §15 수치에 달려 있어 테스트가 단정할 일이 아니기 때문입니다.

### 11:45 KST — 런 화면에 누를 것을 붙이고, 그 길에서 나온 버그 셋

§19.2의 동작 코드 열 개 중 **여덟 개는 만들어지는 곳이 없었습니다.** 지도의
칸 버튼도, 전투의 카드 선택도, 보상 수령도, 상점 구매도, 이벤트 분기도 —
처리하는 핸들러는 다 있는데 그것을 부를 컴포넌트를 아무도 그리지 않았습니다.
스레드에는 그림과 글자만 나가고 누를 것이 없었습니다.

기존 테스트가 통과하고 있던 이유는 핸들러를 **합성한 `custom_id`** 로 직접
불렀기 때문입니다. 화면이 그 `custom_id` 를 실제로 내놓는지는 아무도 보지
않았습니다. 새 테스트(`tests/test_controls.py`)는 합성하지 않고, 화면이 내놓은
것을 그대로 누릅니다.

배선을 깔면서 같은 흐름에서 세 가지가 더 나왔습니다.

**런이 `preparing` 에서 나오지 않았습니다.** `preparing → map_navigation` 은
§16.1의 합법 전이로 선언만 되어 있고 그것을 수행하는 코드가 없었습니다.
지도의 첫 칸을 눌러도 게이트가 거절했습니다. 스레드가 열린 시점([6] SURFACE)에
옮깁니다 — 열지 못했으면 옮기지 않습니다. `recover_runs` 가 `preparing` 인
런을 `retry_surface` 로 분류해야 하기 때문입니다.

**`_resolve_card` 가 평범한 카드 한 장에 UnboundLocalError 를 냈습니다.**
`conclusion` 이 `if ended:` 안에서만 대입되는데 밖에서 읽힙니다. 전투가 끝나지
않는 대부분의 카드가 여기서 터졌고, 그러면 이벤트가 기록되지 않아 중앙봇이
같은 조작을 무한히 재전송합니다 (§16.7). 핸들러를 통해 카드를 내보는 테스트가
하나도 없어서 드러나지 않았습니다.

**`offer_reward` 가 보상을 조용히 없앴습니다.** 이 연산자는 스펙도 있고 §10.5
검증도 통과하고 대시보드에서 이벤트에 넣을 수도 있었는데, 그것이 만든 보류
선택을 처리하는 코드가 없었습니다. 저주받은 카드 제거용 화면이 그것을 대신
받아 "제거할 저주받은 카드가 없습니다" 라고 답하고 끝냈습니다. 보류 선택의
종류를 보고 갈라지도록 고치고, `reward_tables`(스키마에서 유일하게 읽지도
쓰지도 않던 테이블)를 읽어 보상 칸과 **같은 모양** 의 선택으로 바꿉니다 —
그러면 이미 있는 화면과 수령 코드가 그대로 처리합니다. 쓰는 이벤트도 하나
넣었습니다. 쓰는 콘텐츠가 없으면 경로가 깨져 있어도 드러나지 않습니다.

§10.5에 보상 목록 검사를 더했습니다: 가리키는 목록이 있는지, 그 안의 카드가
실재하는지, weight 가 0이 아닌지, 그리고 `offer_reward` 뒤에 다른 효과가 없는지
— 수령이 끝나면 런이 지도로 돌아가므로 그 뒤의 효과는 실행되지 않습니다.

검증: `pytest -q` 결과 695개 테스트 통과(신규 29개).

### 11:00 KST — 코인 잔액을 화면에 보여준다

성급 상승·카드 강화·연구·허브 상점·장비 다섯 화면이 전부 코인 가격을 찍고
있었는데, **"내가 얼마 있는지"는 어디에도 없었습니다.** 살 수 있는지 없는지를
눌러 봐야 아는 화면이었다는 뜻입니다. `CentralClient.get_user` 는 정확히 이
용도로("Balance reads are UX only") 구현되어 있었고 부르는 곳이 없었습니다.

- 표시 전용입니다 (§17.3). **이 값으로 버튼을 막지 않습니다** — 여기서 읽은
  값과 결제 시점 사이에 다른 미니게임이 차감할 수 있고, 권위 있는 검사는
  언제나 §17 트랜잭션 안에서 중앙봇이 합니다.
- 중앙봇이 죽어 있어도 화면은 뜹니다. 읽지 못하면 `코인 —` 로 표시합니다.
- 사용자 응답 스키마도 저장소에 없어서, 스레드 때와 같은 방식으로 흔한 키를
  순서대로 보고 하나도 못 찾으면 WARNING 로그로 드러냅니다
  (`handlers.COIN_BALANCE_KEYS`).

검증: `pytest -q` 결과 666개 테스트 통과(신규 5개). 코인이 0이어도 성급 상승
버튼이 사라지지 않는 것을 테스트로 못 박았습니다 — 표시용 값으로 조작을
막기 시작하면 §17.3이 무너집니다.

### 10:40 KST — 끝난 런의 화면을 마지막 상태로 고쳐 쓴다

§16.6의 프레임 큐(`enqueue_frame` / `next_frame` / `mark_frame`)와
`CentralClient.edit_message` 는 둘 다 구현되어 있으면서 부르는 곳이 하나도
없었습니다. 스레드 배선과 같은 종류의 빈자리입니다.

그래서 런이 **방치로 만료되거나 채널에서 포기되면** 플레이어의 스레드에는
지도와 살아 있는 버튼이 그대로 남았습니다. 누르면 §1.3.10 게이트가 막아 주니
망가지지는 않지만, 왜 안 되는지는 아무 데도 쓰여 있지 않습니다. 포기는 채널에서
하고 화면은 스레드에 있으니, 평범한 흐름에서도 매번 그랬습니다.

- `surfaces.push_frame` / `close_run_surface` — 큐에 넣고, 보내고, 성공했을
  때만 기록합니다.
- **`presentation_revision` 은 보내기 전에 올리지 않습니다.** §16.7 CAS 가
  그 번호의 유일한 주인이고, 실패했는데 번호만 올라가면 그 뒤의 플레이어
  조작이 전부 STALE_REVISION 으로 거절됩니다.
- 끝난 화면에서는 버튼을 걷어냅니다. 게이트가 막아 주더라도 눌리는데 안 되는
  버튼을 남겨 둘 이유가 없습니다.
- 진행 중인 런에는 쓰지 않습니다. 인터랙션 응답이 이미 그 화면의 기록자이고,
  둘이 되면 §16.6이 막으려던 바로 그 경합이 됩니다.
- 기동 시 만료 정산에도 붙였습니다. 서비스가 내려가 있는 동안 만료된 런은
  플레이어가 없는 자리에서 끝나므로, 알릴 방법이 이것뿐입니다.

검증: `pytest -q` 결과 661개 테스트 통과(신규 8개). 배선을 다시 빼고 돌려
포기·만료 두 테스트가 실제로 실패하는 것을 확인했습니다.

### 10:05 KST — 런 스레드를 실제로 만들도록 배선

**`!덱아웃 시작` 이 스레드를 만들지 않고 있었습니다.**

`screens.surface_request` 는 §1.3.3대로 delivery intent 를 기록하고 응답에
`thread_request` 를 담아 돌려주고 있었습니다. 그런데 그 요청을 실제 호출로
바꾸는 코드가 어디에도 없었습니다 — `CentralClient.create_thread` 는 정의만
되어 있고 부르는 곳이 하나도 없었습니다. `grep thread_request` 의 결과가
정의 한 줄뿐이었던 것이 단서였습니다.

그 결과는 `handlers._prepare_and_materialize` 의 주석이 이미 경고하고 있던
바로 그것입니다: 런 행은 생기고 `runs.thread_id` 는 NULL 로 남습니다. 이후의
`!덱아웃` 은 매번 `recreate_surface()` 로 세대 번호만 올릴 뿐 스레드를 다시
만들지 않고, §16.3의 "계정당 하나" 규칙 때문에 그 계정은 **새 런도 시작할 수
없는 상태로 잠깁니다.** 실배포에서 게임이 `시작` 이후로 진행되지 않습니다.

테스트가 전부 통과하고 있던 이유는 핸들러와 엔진 함수를 직접 부르고 있어서
`/event` 응답 파이프라인을 지나지 않았기 때문입니다.

- `app/central/surfaces.py` — 핸들러는 "스레드가 필요하다"고 응답에 적기만
  하고, 실제 호출은 `/event` 파이프라인 한 곳에서 합니다. 핸들러마다 중앙봇
  클라이언트를 들고 다니지 않아도 되고 고칠 곳이 하나입니다.
- 결과는 `delivery.handle_delivery_result` 를 통해 기록합니다. 멱등성 검사와
  §16.6 staleness 판정이 그 안에 있어서, 두 경로가 서로 다르게 바인딩을 쓰기
  시작하면 어느 쪽이 맞는지 알 수 없게 됩니다.
- §16.8 재생성도 같이 고쳤습니다. `recreate_surface()` 는 세대만 올리고
  `recreate_thread` 를 부르지 않아서, 스레드가 지워진 런은 세대 번호만 계속
  오르고 화면은 영영 돌아오지 않았습니다. 허브가 이제 실제로 다시 열고 그
  스레드로 안내합니다.
- 실패해도 런은 그대로 둡니다. 같은 `surface_generation` 으로 다시 부르는
  것은 멱등하므로(§1.3.5) 다음 `!덱아웃` 이 이어서 시도합니다.
- `DECKOUT_CHANNEL_ID` 가 없으면 기동을 거부합니다. 채널이 없으면 스레드를
  만들 수 없고, 그 상태로 서비스를 여는 것은 플레이어가 먼저 알게 되는
  문제를 만드는 일입니다.

연동 가이드의 스레드 생성 **응답 스키마**는 이 저장소에 없습니다(§1.1 표에
경로만 있습니다). 그래서 흔한 키 이름 몇 개를 순서대로 보고, 하나도 없으면
조용히 넘어가지 않고 실제로 무슨 키가 왔는지 ERROR 로그로 남깁니다. 가이드를
확인하면 고칠 곳은 `surfaces.THREAD_ID_KEYS` 하나입니다.

검증: `pytest -q` 결과 649개 테스트 통과(신규 13개). 새 테스트는 핸들러가
아니라 `/event` 를 통과시켜 확인합니다 — 배선이 빠져 있던 곳이 거기였기
때문입니다. 배선을 다시 빼고 돌려 그 테스트가 실제로 실패하는 것까지
확인했습니다.

### 09:30 KST — 패시브·출석·월드 콘텐츠·자리를 지킬 그림

README 의 "구현하지 않은 것" 목록을 비우는 작업입니다. 네 갈래로 나눠
커밋했습니다.

**§6 패시브 카드 (`535a90f`).** 준비 화면의 [3] 패시브 선택 단계도, 슬롯도,
연구 해금도 이미 있었는데 정작 고를 것이 하나도 없었습니다 —
`selectable_passives()` 가 빈 목록을 돌려주고 있었고 그래서 그 단계는 매번
그냥 건너뛰어졌습니다.

- `passive_cards` / `unlocked_passives` / `battle_passives` (스키마 v4).
  패시브를 `cards` 에 넣지 않은 것은, 넣으면 코스트·원소·대상 세 열이 전부
  뜻 없는 값으로 채워지고 손패를 뽑는 모든 질의가 "패시브는 빼고" 라는
  조건을 달고 다녀야 하기 때문입니다.
- 가챠 해금 — §5.1이 확정한 캐릭터:카드 3:7 은 건드리지 않고, **카드 몫
  안에서** `gacha_passive_share_of_cards` 만큼 갈라집니다. 중복은
  와일드카드로 바꿉니다(패시브에는 업그레이드가 없어 조각을 줄 곳이 없습니다).
- 전투 시작과 라운드 시작에 발동합니다. `duration_rounds = -1` 이 "전투가
  끝날 때까지" 를 뜻하도록 `timed_effects` 에 사전을 하나 추가했습니다 —
  라운드 수를 크게 적어 두는 것과 달리 긴 전투에서 조용히 꺼지지 않습니다.
- 전투 시작 시점의 장착을 `battle_passives` 에 찍습니다. §16.2.3과 같은
  이유로, 전투 중에 런의 장착이 바뀌어도 그 전투는 시작할 때의 구성으로
  끝나야 합니다.
- 출시 세트 8장. 등급 1~6이 모두 나오고, "상시" 와 "전투 중 조건부" 두
  발동 방식이 모두 쓰이고, 파티 대상과 적 대상이 모두 있습니다. §6이 확정한
  것은 구조뿐이고 효과 목록은 문서에 없으므로, 이 8장은 구조를 빠짐없이
  밟는 출시용 세트이지 확정된 기획이 아닙니다.
- §10.5 — 패시브에는 시전자가 없으므로 시전자의 능력치(`deal_damage`)나
  손패(`draw_cards`)에 기대는 연산자는 전투 중이 아니라 **저장할 때**
  막습니다. 운영자가 대시보드에서 잘못 넣었을 때 몇 시간 뒤 전투 중에
  알게 되어서는 안 됩니다.

**§13.2 출석 보상 (`76ba668`).** `daily_claims` 테이블만 있고 그 위에 아무것도
없었습니다. 하루의 경계와 빠뜨린 날의 규칙이 문서에 없으므로, 임의로 정하는
대신 설정으로 뺐습니다.

- `daily_reset_hour_kst`(기본 4시), `daily_streak_grace_days`(기본 1),
  `daily_rewards`(줄 수가 곧 주기의 길이).
- 연속 일수는 저장하지 않고 기록에서 매번 다시 셉니다. 저장해 두었다면
  규칙을 바꾼 순간 옛 값과 새 규칙이 뒤섞였을 것입니다.
- 코인은 §17 grant 트랜잭션으로 나가고 키가 `daily:{user}:{날짜}` 라서,
  같은 날의 재시도는 구조적으로 두 번 지급될 수 없습니다. `local_handlers()`
  에도 등록했습니다 — 없으면 코인만 나가고 기록이 유실됩니다 (§17.4).
- `carta_income` 의 `daily` / `weekly_attendance` 는 지웠습니다. 같은 것을 두
  곳에서 정하게 두면 어느 쪽이 진짜인지 알 수 없습니다.

**자리를 지킬 그림 (`79e33cf`).** 파이프라인은 있는데 그 안이 비어 있었습니다.
§11의 대체 그림은 게임이 멈추지 않는 것을 보장하지만, 한 화면에 단색 판이
열두 개 늘어서면 무엇이 무엇인지 알아볼 수 없습니다. `python -m
app.cli.make_art` 가 id 를 해시해 색과 무늬를 정하므로 같은 id 는 언제나 같은
모양이고 다른 id 는 확실히 다릅니다. 이미 있는 파일은 덮지 않습니다.

**월드 콘텐츠 (`fcd5126`).** 여기서 진짜 구멍이 하나 나왔습니다. **2·3·4세계에는
적도 조우도 하나 없었고, 1세계에는 보스 조우가 없었습니다.** 정산이 클리어할
때마다 다음 세계를 열어 주므로, 튜토리얼을 마친 계정은 1세계 보스 칸에서 런이
죽습니다. 엔진은 그 자리에서 "§10.5가 막았어야 한다"는 오류를 냈는데 정작
§10.5에 그 검사가 없었습니다. **검사를 먼저 넣고**, 걸린 구멍을 콘텐츠로
메웠습니다: 적 11종, 조우 14개, 본편 보스 4종, 적 행동 8개, 카드 11장,
캐릭터 3명, 장비 6개와 세트 2개, 이벤트 4개, 업적 7개.

같은 커밋에서 `curse_removed` 와 `character_starred` 훅을 달았습니다. 두
카운터는 선언만 되어 있고 아무 데서도 오르지 않아서, 그 카운터를 쓰는 업적은
영원히 0이었습니다. 성급 상승 페이로드에 콘텐츠 버전을 넣되 키가 없는 옛
트랜잭션 행은 업적만 건너뜁니다 — 업적 하나 때문에 이미 코인을 낸 성급
상승을 실패시킬 수는 없습니다 (§17.4).

엘리트 조우도 함께 넣었습니다. `_post_battle_rewards` 는 이미 `elite_clear`
라는 별도 보상 줄을 갖고 있었는데, 엘리트 조우를 고를 방법이 어디에도 없어서
그 줄이 영영 쓰이지 않고 있었습니다. 전투 칸이 깊은 곳에서 엘리트로 바뀔 수
있게 열되, **확률의 기본값은 0** 입니다 — 문서가 이 규칙을 정해 두지 않았으므로
켤지 말지는 오너의 몫이고, 0이면 동작이 종전과 정확히 같습니다.

검증: `pytest -q` 결과 636개 테스트 통과(신규 69개).
`python -m app.cli.bootstrap` 과 `python -m app.cli.check_content` 를 새
데이터베이스에서 다시 돌려 §10.5 통과를 확인했고, 2~4세계에서 실제로 전투가
붙는지와 네 세계 모두 보스 조우를 고를 수 있는지를 테스트로 확인했습니다.

### 19:20 KST — 관리자 대시보드 (§10.1–10.3)

같은 프로세스의 `/admin` 에 붙는 서버 렌더 대시보드를 구현했습니다.
중앙봇 계약(§1.3)과 완전히 분리된 경로라 `/event` 동작에 영향을 주지 않습니다.

- **비밀번호가 없으면 켜지지 않습니다.** 기본 비밀번호를 두지 않았습니다 —
  설정을 잊은 배포가 열린 관리자 화면으로 뜨는 것보다 안 뜨는 편이 안전합니다.
  세션은 표준 라이브러리 hmac 서명 쿠키이고, 비밀번호 비교는 상수 시간입니다.
- **편집은 언제나 초안으로 갑니다.** 발행된 버전은 §10.6의 불변 스냅샷이고
  진행 중인 런이 그 버전을 붙들고 있어서, 값을 바꾸면 런 도중에 내용이
  달라집니다. 초안이 없으면 현재 버전을 **복사해** 만듭니다 — 빈 초안을
  발행하면 콘텐츠가 통째로 사라지기 때문입니다.
- 화면: 현황 / 밸런싱 / 콘텐츠(22개 테이블) / 그림 / 버전 / 플레이어.
- 밸런싱 화면은 `config/*.toml` 의 설명을 그대로 보여줍니다. 코드를 몰라도
  무엇을 바꾸는 값인지 알 수 있어야 이 화면이 쓸모가 있습니다.
- 그림을 브라우저에서 바로 올릴 수 있고, 올린 파일은 다음 화면부터 쓰입니다.
- 논리 키는 편집할 수 없게 막았습니다. 플레이어 데이터가 그 id를 참조하므로
  이름을 바꾸면 참조가 끊깁니다 (§10.6).
- 런 강제 종료(`admin_terminated`)는 사유를 반드시 받습니다. 정상적인
  끝맺음이 아니라 운영 개입이므로 그 사실이 남아야 합니다.

**§10.5 검증에 §15 상수 검사를 추가했습니다.** 대시보드로 상수를 고칠 수 있게
되었는데 검증이 그쪽을 전혀 보지 않아서, 확률의 합이 1이 아닌 표를 발행할 수
있었습니다. 파일을 고치는 사람은 `tests/test_config.py` 가 잡아 주지만
대시보드로 고치는 사람에게는 발행 시점의 검사가 유일한 안전망입니다. 확률 합,
기본 덱 구성 합, 지도 칸 수, 와일드카드 시작 지점, 카드 비용 상한, 튜토리얼
보상, 뽑기 희귀도 커버리지, 장비 등급 표를 봅니다.

**의존성을 늘리지 않았습니다.** 세션은 stdlib hmac, 변경은 JSON POST, 그림
업로드는 원본 바이트 POST라 `python-multipart` 도 `itsdangerous` 도 필요
없습니다. 봇 서비스가 나갈 수 있는 환경을 좁히지 않기 위해서입니다.

고친 것: `hmac.compare_digest` 에 비ASCII 문자열을 넘기면 TypeError가 나므로
한글 비밀번호를 쓴 배포에서 로그인이 500으로 죽었습니다. 바이트 비교로
바꿨습니다.

검증: `pytest -q` 결과 567개 테스트 통과(신규 35개). 실제로 서비스를 띄워
로그인 → 상수 편집 → 검증 실패 확인 → 수정 → 발행까지 걸어 보고, 발행 직후
서비스가 새 버전을 읽는 것(`/healthz` 의 `content_version_id`)까지 확인했습니다.

### 17:35 KST — §16.3 방치 만료가 실제로 발동하도록 수정

`is_expired()` 는 처음부터 있었지만 `recover_runs()` 안에서만 불렸고, 그 결과는
계획으로 반환되어 **아무도 실행하지 않았습니다.** 게다가 그 검사는 서비스
기동 시 단 한 번뿐이었습니다.

그동안 방치된 런 하나가 §16.3의 계정당 하나 규칙으로 그 계정의 새 런을
영구히 막았습니다. `!덱아웃 포기` 명령을 아는 사람만 스스로 풀 수 있었고,
스레드가 삭제된 경우에는 그것도 어려웠습니다. `inactivity_expiry_minutes`
설정값은 아무 일도 하지 않는 죽은 값이었습니다.

- `lifecycle.expire_run()` / `expire_if_stale()` 을 추가했습니다. 만료는
  `포기`와 같은 정산 경로를 지나므로 §15.10 보존 밴드가 그대로 적용됩니다 —
  방치가 포기보다 이득이 되지 않습니다.
- 서비스에 주기 작업이 없으므로, 계정이 막혀 있는지가 실제로 문제가 되는
  순간에 확인합니다: 허브 열기, `!덱아웃 시작`, 준비 화면.
- 기동 시 계획에서 `settle_expired` 항목을 실제로 처리합니다. 하나가 실패해도
  나머지를 계속합니다 — 만료 정산에 걸려 서비스가 못 뜨면 막힌 계정을 풀어
  줄 방법마저 사라집니다.
- 정리했을 때는 허브 화면이 그 사실을 알립니다. 조용히 지우면 플레이어는
  자기 런이 어디 갔는지 알 수 없습니다.
- 상점 나가기 핸들러가 상태를 직접 UPDATE 하던 것을 `nodes.leave_shop()` 으로
  바꿨습니다. 같은 규칙을 두 곳이 따로 들고 있으면 언젠가 갈라집니다.

검증: `pytest -q` 결과 532개 테스트 통과(신규 13개). 만료가 판정되는지가
아니라 **일어나는지**, 그리고 진행 중인 런까지 걷어내지는 않는지를 함께
확인합니다.

### 16:10 KST — 허브 화면에 실행 버튼 추가, `덱` 화면 구현

허브 화면이 전부 읽기 전용 목록이라, 엔진에 구현된 진행 시스템 전체가
플레이어에게 닿지 않고 있었습니다. 성급 상승(§4.4), 카드 업그레이드(§5.8,
v6.4의 P-1), 연구(§20.5), 허브 상점(§7.2), 장비 강화·장착(§8.4) 모두
`progression.py` 에 있으면서 어떤 화면도 호출하지 않았습니다.

- `app/api/hub.py` 를 추가하고 `dko:hub:` 접두사로 라우팅했습니다. 준비·뽑기
  화면과 같은 방식입니다 — 허브는 런 밖이라 §19.2의 `custom_id` 형식(run_id를
  요구)을 쓸 수 없습니다.
- **트랜잭션 키를 이벤트에서 유도합니다.** 서버는 핸들러가 돌아온 *뒤에*
  `processed_events` 를 기록하므로(§16.7), 차감 직후 죽으면 중앙봇이 같은
  이벤트를 다시 보냅니다. 그때 새 키를 만들면 두 번 청구됩니다 — §17.3
  규칙 2가 금지하는 그것입니다. 재전송 시 중복 청구되지 않는지, 그리고
  멱등성이 지나쳐 서로 다른 두 구매를 삼키지는 않는지 양쪽을 테스트합니다.
- 재화가 실제로 충분한 것만 선택지에 올립니다. 눌러도 거절되는 항목을
  늘어놓으면 목록만 길어집니다.
- 거절될 때는 화면을 그대로 두고 사유만 알립니다. 목록이 사라지면 무엇이
  부족했는지 확인할 수 없습니다.
- §17 결과를 그대로 옮겨 적습니다 — `rejected_no_charge`(청구 안 됨)와
  `compensated`(환불됨)를 성공처럼 보여주지 않습니다.
- `!덱아웃 덱` 화면을 구현했습니다. 런 중이면 자리별 뽑을/버린 더미와 저주
  카드 수를, 런 밖이면 소장 카드(캐릭터 카드 + 행동 카드)와 강화 선택지를
  보여줍니다. 런 중에는 강화 선택지를 내밀지 않습니다 — §16.2.3대로 이 런에
  반영되지 않으므로 누르게 하면 오해를 부릅니다.
- 소장 화면을 Pillow로 그립니다(`render_collection`). 캐릭터 카드를 앞에 두고
  테두리로 구분합니다.

검증: `pytest -q` 결과 519개 테스트 통과(신규 14개).

### 14:40 KST — 캐릭터도 카드로, 그리고 런 시작 전 덱 구성

캐릭터와 카드를 서로 다른 것처럼 다루던 부분을 하나의 카드 목록으로 합치고,
런을 시작하기 전에 덱을 직접 구성하는 단계를 넣었습니다.

- `app/content/catalog.py` — 플레이어가 보는 것은 전부 카드입니다. 그중
  **캐릭터 카드**가 파티 자리를 차지하고, **행동 카드**가 그 캐릭터의 덱에
  들어갑니다. 저장은 여전히 `characters` / `cards` 두 테이블이지만(성급과
  업그레이드가 다르게 자라고, §10.6 버전 고정과 §16.2.3 스냅샷이 이 구분
  위에 서 있습니다), 그 차이를 아는 것은 이 모듈 하나뿐입니다.
- 준비 화면에 [3] 덱 구성 단계를 넣었습니다. 파티 자리마다 그 캐릭터가
  낼 수 있는 카드(§2.10 — 속성이 맞거나 무속성)를 고르고, 고르지 않으면
  종전대로 자동 구성합니다. 튜토리얼은 고를 것이 사실상 없어 그대로
  자동 구성으로 지나갑니다.
- §4.3의 기본 카드 몫은 어떤 선택으로도 없앨 수 없습니다. 낼 카드가 없어
  자동 방어만 하게 되는 덱을 만들 수 있어서는 안 되기 때문입니다.
- 화면이 보여주는 덱과 실제로 만들어지는 덱을 같은 함수(`preview_deck`)로
  뽑습니다. 둘이 갈라지면 확정 버튼을 누를 때까지 자기 덱을 알 수 없게
  됩니다. 테스트로 두 결과가 같은지 확인합니다.
- 파티를 다시 고르면 이미 고른 덱을 버립니다 — 자리 번호가 가리키는
  캐릭터가 달라지기 때문입니다.
- `run_drafts.deck_json` 추가(스키마 v3, 전진 전용 마이그레이션).

검증: `pytest -q` 결과 505개 테스트 통과(신규 17개).

### 13:20 KST — 직접 넣는 그림(PNG)과 Pillow 화면 렌더

`assets/` 아래에 PNG를 두면 그대로 쓰이고, 상점·배너·뽑기 결과·전투·지도·
준비 화면을 모두 Pillow로 그리도록 했습니다.

- `app/render/assets.py` — `assets/<폴더>/<id>.png` 규칙으로 그림을 찾습니다.
  파일 수정 시각을 캐시 키에 넣어, 파일을 바꾸면 다음 화면부터 바로 새
  그림이 나갑니다. 없거나 깨졌거나 너무 크면 이름과 희귀도 색으로 대신
  그립니다 — 그림이 하나도 없어도 게임은 정상 동작합니다(§11).
- `app/render/theme.py` — 크기·색·글꼴을 `config/10_화면.toml` 에서 읽습니다.
  겉모습은 게임 결과에 영향이 없으므로 콘텐츠 버전에 고정하지 않고 파일에서
  곧장 읽습니다. 게임 수치는 종전대로 `Balance` 를 거쳐 버전에 고정됩니다.
- 글꼴은 파일 존재 여부가 아니라 **한글이 실제로 그려지는지**를 확인해
  고릅니다. DejaVu 처럼 설치는 되어 있지만 한글 글리프가 없는 글꼴이 뽑혀
  이름이 전부 네모로 나오는 문제가 있었습니다.
- `app/render/panels.py` — 카드/손패/상점/배너/뽑기 결과/준비 화면을 새로
  그리고, 기존 전투·지도·정산 화면도 설정에서 크기와 색을 읽도록 고쳤습니다.
  코드에 수치가 남지 않았습니다.
- `app/api/visuals.py` — 게임 상태를 화면 그림으로 바꾸는 층. 렌더 실패를
  삼켜서, 그림을 못 만들어도 글자만으로 화면이 나갑니다. 여기서 예외가 새면
  조작이 통째로 되돌아가고 중앙봇이 이벤트를 무한 재전송합니다(§16.7).
- 핸들러 응답에 `attachments` 를 실어 실제 화면에 붙였습니다. 렌더러가
  있는 것과 화면에 붙는 것은 다른 문제라, 전투·지도·상점·뽑기·준비 화면
  각각에 대해 실제로 붙는지 테스트했습니다.
- `python -m app.cli.assets` 로 어떤 그림이 필요하고 무엇이 아직 없는지,
  넣은 파일 중 못 쓰는 것이 무엇인지 확인할 수 있습니다.

검증: `pytest -q` 결과 488개 테스트 통과(신규 21개). 전 화면을 실제로
렌더해 §1.3.7 한계(4 MiB, 4096×4096) 안에 드는 것을 확인했습니다.

### 11:05 KST — 밸런싱 수치를 `config/*.toml` 로 분리

모든 게임 수치를 게임 플레이 영역별 설정 파일로 옮기고, 설정마다 코드를
몰라도 이해할 수 있는 설명을 붙였습니다.

- `config/` 에 9개 파일로 나눴습니다: 전투 / 상태이상 / 적 행동 / 뽑기 /
  성장 / 경제 / 맵과 이벤트 / 튜토리얼 / 런 운영. 뽑기 확률만 만지려는
  사람이 전투 수식을 지나칠 일이 없게 하려는 분리입니다.
- 설명은 설정 바로 위의 주석이 원본이며, `config_loader.parse_docs()` 가
  그대로 읽어 `balancing_metadata` 테이블에 심습니다. 파일과 대시보드의
  설명이 갈라지지 않습니다.
- `python -m app.cli.show_config [검색어]` 로 파일을 열지 않고 설정과
  설명을 볼 수 있습니다.
- 엔진에 남아 있던 게임 수치 하드코딩을 걷어내고 설정 17개를 새로
  뽑았습니다: 지도의 갈림길·상점 최소 깊이·휴식 간격, 뽑기 등급별 성급과
  희귀도, 카드 업그레이드 전이별 게이트와 와일드카드 시작 지점, 보상 희귀도
  구간, 깊이별 장비 등급, 상점 회복량, 전투 자동 진행 상한.
- TOML에는 빈 값이 없어 `"없음"` 을 약속된 표기로 정했습니다
  (상태이상의 `duration`, `stack_cap`).

값은 여전히 `balancing_constants` 를 통해 콘텐츠 버전에 고정된 채로 읽히므로,
진행 중인 런은 파일을 고쳐도 시작할 때의 수치를 그대로 유지합니다(§10.6).

검증: `pytest -q` 결과 467개 테스트 통과(신규 22개). 설정 99개가 모두
이전 기본값과 정확히 일치함을 확인했습니다.

### 09:40 KST — 준비 화면([1]~[6])과 뽑기 화면 구현

`app/api/screens.py`를 추가하고 핸들러에 연결했습니다. 이 작업 전에는 튜토리얼을
졸업한 계정이 본편에 들어갈 방법이 없었습니다(본편은 파티 2명 필요, 계정에는
캐릭터 1명, 두 번째를 얻는 유일한 경로인 뽑기 화면이 미구현).

- §16.2.2 [1] 월드 → [2] 파티 → [3] 패시브 → [4] 확정 → [5] 런 생성 →
  [6] 스레드 생성 요청까지의 흐름을 구현했습니다. [1]~[4]는 런 행을 만들지
  않으므로 `run_drafts` 테이블에 초안만 보관합니다.
- [5]와 [6]을 분리하지 않도록 `surface_request()`로 합쳤습니다. `runs.thread_id`를
  쓰는 유일한 경로가 `message_delivery_result` 콜백(§1.3.3)이라, 스레드 생성을
  요청하지 않은 런은 화면 없이 계정만 점유해 §16.3 규칙상 새 런도 막습니다.
- 뽑기 화면을 구현하고, 중앙봇 재전송에 대비해 `gacha_id`를 이벤트 id에서
  파생시켜 §5.9 재생 경로가 실제로 동작하도록 했습니다.
- `gacha.pull`이 카르타 잔액을 트랜잭션 안에서 다시 읽도록 고쳐, 동시 클릭 시
  이중 차감을 막았습니다.
- §6 패시브 선택 목록의 정의를 `lifecycle.selectable_passives()` 한 곳으로
  모으고, 화면과 `validate_build`가 같은 목록으로 검사하도록 했습니다.
  `custom_id`는 위조 가능하므로 개수만 세는 기존 검사로는 부족했습니다.
  (§6 패시브 콘텐츠는 아직 작성되지 않아 현재 목록은 비어 있고, 이 단계는
  의도적으로 건너뜁니다.)

검증: `pytest -q` 결과 445개 테스트 통과.

### 00:09 KST — 변경 이력 도입

- 여러 작업자가 변경 내용을 추적할 수 있도록 `CHANGELOG.md`를 추가했습니다.
- 이후 Codex가 수행하는 의미 있는 변경은 이 파일에 먼저/함께 기록합니다.

### 00:05 KST — 중앙봇 HTTP 연동 계약 반영

- 중앙봇의 `POST /event` 메시지 형식(`raw_content`, `command`, `args`)을 처리하도록 변경했습니다.
- 응답을 중앙봇 action 형식(`reply`, `ignore`, `reply_ephemeral`)과 PNG `attachments[].data_b64` 형식으로 전환했습니다.
- `GET /healthz`, `POST /shutdown`을 추가했습니다.
- Economy API 호출을 `X-API-Key`, `idempotency_key`, 실제 적용 금액 검증 방식으로 변경했습니다.
- 명령 루트를 `!덱아웃`으로 전환하고, 기존 `!카드`는 호환용 별칭으로 유지했습니다.
- 중앙봇 계약 및 Economy 호출 테스트를 추가했습니다.

검증: `pytest -q` 결과 67개 테스트 통과.

커밋: `83a4e75 Integrate Central Bot HTTP contract`
